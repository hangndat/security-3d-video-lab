---
phase: 23-tls-3d-production-export
plan: 02
type: execute
wave: 1
depends_on:
  - 23-01
files_modified:
  - tests/tls-production-export.test.ts
  - scripts/verify-tls-3d-production.mjs
  - scripts/verify-tls-production.mjs
  - docs/tls-crew-walkthrough.md
  - package.json
  - src/render/export/generate-tls-production-artifacts.ts
autonomous: true
requirements:
  - RENDER-03
must_haves:
  truths:
    - "GL-gated test proves local default export uses r3f-headless and frameSource png."
    - "Full TLS production MP4 passes assertExportQuality on 3D path when GL available."
    - "verify-tls-3d-production.mjs writes phase23/tls-3d-production.json with gateStatus pass."
    - "Crew walkthrough documents video-only v1.5 publish path."
    - "TLS security sign-off beats still pass on video-only 3D export."
  artifacts:
    - path: "scripts/verify-tls-3d-production.mjs"
      provides: "RENDER-03 verification gate."
    - path: ".artifacts/verification/phase23/tls-3d-production.json"
      provides: "Machine-readable Phase 23 evidence."
  key_links:
    - from: "verify-tls-3d-production.mjs"
      to: "tests/tls-production-export.test.ts"
      via: "3D export smoke tests"
      pattern: "tls-3d-production"
    - from: "docs/tls-crew-walkthrough.md"
      to: "generate-tls-production-artifacts.ts"
      via: "Video-only Step 5 handoff"
      pattern: "videoOnly"
---

<objective>
Complete RENDER-03: 3D production smoke tests, verification gate, and crew documentation for video-only TLS publish.

Purpose: Machine-verifiable proof that TLS production MP4 is PNG-backed locally before Phase 24 CI strategy.
Output: GL-gated tests, verify script, walkthrough updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-tls-3d-production-export/23-CONTEXT.md
@src/render/export/generate-tls-production-artifacts.ts
@scripts/verify-tls-production.mjs
@scripts/verify-headless-scene-parity.mjs
@docs/tls-crew-walkthrough.md
@src/verification/export-quality.ts

<interfaces>
Verify JSON shape:
```typescript
{
  generatedAt: string;
  gateStatus: "pass" | "fail";
  blockingCriteria: {
    videoOnlyManifestTestsPassed: boolean;
    tls3dSmokeTestsPassed: boolean;
    moduleRubricPassed: boolean;
  };
  manifestChecks: {
    expectsVideoOnly: true;
    expectsFrameSourceWhenR3f: "png";
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GL-gated 3D production smoke tests</name>
  <read_first>
    - tests/tls-production-export.test.ts
    - tests/headless-capture.test.ts
  </read_first>
  <files>
    - tests/tls-production-export.test.ts
  </files>
  <action>
    Add describe.skipIf(!glAvailable) block RENDER-03: generateTlsProductionArtifacts with default env (no trace-hash override) → manifest.renderBackend === r3f-headless, frameSource === png, videoOnly === true; assertExportQuality on output MP4. Add contrast test with SECURITY_LAB_RENDER_BACKEND=trace-hash → frameSource ppm-trace-hash (no GL required). Keep existing PROD-01 tests passing via INCLUDE_NARRATION for narration-specific cases or split describe blocks.
  </action>
  <acceptance_criteria>3D smoke passes when GL available; trace-hash contrast test passes without GL.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts</automated>
  </verify>
  <done>3D vs trace-hash export paths tested.</done>
</task>

<task type="auto">
  <name>Task 2: verify-tls-3d-production gate and npm script</name>
  <read_first>
    - scripts/verify-tls-production.mjs
    - scripts/verify-headless-scene-parity.mjs
  </read_first>
  <files>
    - scripts/verify-tls-3d-production.mjs
    - package.json
    - scripts/verify-tls-production.mjs
  </files>
  <action>
    Create verify-tls-3d-production.mjs: run tls-production-export tests filtered to RENDER-03/videoOnly/3d patterns; write `.artifacts/verification/phase23/tls-3d-production.json`. Add npm script verify:tls-3d-production. Optionally extend verify-tls-production.mjs to read manifest frameSource when artifact present (non-blocking in --quick). Gate passes when non-GL tests pass (GL tests skip gracefully).
  </action>
  <acceptance_criteria>node scripts/verify-tls-3d-production.mjs --quick exits 0.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-tls-3d-production.mjs --quick</automated>
  </verify>
  <done>RENDER-03 verification gate operational.</done>
</task>

<task type="auto">
  <name>Task 3: Crew walkthrough and render handoff updates</name>
  <read_first>
    - docs/tls-crew-walkthrough.md
    - src/render/export/generate-tls-production-artifacts.ts
  </read_first>
  <files>
    - docs/tls-crew-walkthrough.md
    - src/render/export/generate-tls-production-artifacts.ts
  </files>
  <action>
    Update Step 5 and Verification Commands: v1.5 default is video-only 3D export (no narration mux); document verify:tls-3d-production. Update render-handoff.md template to include videoOnly, frameSource, and note audio layer deferred. Step 6 audio section: clarify optional for v1.5 video-only milestone.
  </action>
  <acceptance_criteria>Walkthrough reflects video-only publish path and new verify command.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts --testNamePattern=generateTlsProductionArtifacts</automated>
  </verify>
  <done>Crew docs aligned with RENDER-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI | verify --quick must not require 236-frame 3D render |
| Local dev | Full 3D render acceptable for publish validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | D | Slow CI | mitigate | skipIf GL + trace-hash contrast without GL |
| T-23-04 | T | False 3D claim | mitigate | frameSource png assertion in manifest |
</threat_model>

<verification>
- [ ] `node scripts/verify-tls-3d-production.mjs --quick`
- [ ] `npm run test -- tests/tls-production-export.test.ts`
- [ ] `npm test`
</verification>

<success_criteria>
- RENDER-03: TLS production exports real 3D MP4 locally; machine-verifiable via phase23 JSON gate.
</success_criteria>

<output>
After completion, create `.planning/phases/23-tls-3d-production-export/23-02-SUMMARY.md`
</output>
