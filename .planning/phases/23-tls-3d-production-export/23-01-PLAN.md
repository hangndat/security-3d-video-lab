---
phase: 23-tls-3d-production-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/render/export/generate-tls-production-artifacts.ts
  - src/render/headless/resolve-production-render-backend.ts
  - src/verification/tls-production-rubric.ts
  - tests/tls-production-export.test.ts
autonomous: true
requirements:
  - RENDER-03
must_haves:
  truths:
    - "generateTlsProductionArtifacts defaults to videoOnly export (no narration artifacts)."
    - "Manifest schemaVersion 1.1.0 includes renderBackend, frameSource, videoOnly."
    - "frameSource is png when renderBackend is r3f-headless."
    - "renderCompositionProductionMp4 receives explicit backend from resolver."
    - "assertTlsProductionRubric passes in videoOnly mode without narration alignment."
  artifacts:
    - path: "src/render/export/generate-tls-production-artifacts.ts"
      provides: "Video-only TLS production bundle generator."
    - path: "src/render/headless/resolve-production-render-backend.ts"
      provides: "resolveFrameSource helper."
    - path: "tests/tls-production-export.test.ts"
      provides: "RENDER-03 manifest and video-only tests."
  key_links:
    - from: "generate-tls-production-artifacts.ts"
      to: "capture-viz-frame-png.ts"
      via: "r3f-headless renderCompositionProductionMp4"
      pattern: "renderCompositionProductionMp4"
    - from: "generate-tls-production-artifacts.ts"
      to: "resolve-production-render-backend.ts"
      via: "renderBackend + frameSource in manifest"
      pattern: "resolveFrameSource"
    - from: "tls-production-rubric.ts"
      to: "generate-tls-production-artifacts.ts"
      via: "videoOnly rubric option"
      pattern: "videoOnly"
---

<objective>
Deliver RENDER-03 core: video-only TLS production export with manifest v1.1.0 and r3f-headless default render path.

Purpose: Replace trace-hash as local publish output while preserving CI trace-hash override via env.
Output: Updated artifact generator, frameSource helper, video-only rubric, manifest tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-tls-3d-production-export/23-CONTEXT.md
@.planning/phases/23-tls-3d-production-export/23-RESEARCH.md
@src/render/export/generate-tls-production-artifacts.ts
@src/render/remotion/render-composition.tsx
@src/render/headless/resolve-production-render-backend.ts
@src/verification/tls-production-rubric.ts
@tests/tls-production-export.test.ts

<interfaces>
Frame source helper:
```typescript
export type ProductionFrameSource = "png" | "ppm-trace-hash";

export function resolveFrameSource(
  backend: ProductionRenderBackend
): ProductionFrameSource;
```

Manifest v1.1.0 (video-only):
```typescript
{
  schemaVersion: "1.1.0";
  videoOnly: true;
  renderBackend: "r3f-headless" | "trace-hash";
  frameSource: "png" | "ppm-trace-hash";
  // narrationTrackPath?, narrationProviderId? — optional
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: frameSource helper and explicit backend wiring</name>
  <read_first>
    - src/render/headless/resolve-production-render-backend.ts
    - src/render/remotion/render-composition.tsx
  </read_first>
  <files>
    - src/render/headless/resolve-production-render-backend.ts
    - tests/tls-production-export.test.ts
  </files>
  <action>
    Add `ProductionFrameSource` type and `resolveFrameSource(backend)` mapping png↔r3f-headless, ppm-trace-hash↔trace-hash. Add unit tests for mapping. In generateTlsProductionArtifacts, resolve backend from env and pass `{ backend, captionMap }` explicitly to renderCompositionProductionMp4.
  </action>
  <acceptance_criteria>resolveFrameSource tests pass; render uses explicit backend option.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts --testNamePattern=frameSource</automated>
  </verify>
  <done>Backend and frameSource contract defined.</done>
</task>

<task type="auto">
  <name>Task 2: Video-only artifact generator and manifest v1.1.0</name>
  <read_first>
    - src/render/export/generate-tls-production-artifacts.ts
    - .planning/phases/23-tls-3d-production-export/23-RESEARCH.md
  </read_first>
  <files>
    - src/render/export/generate-tls-production-artifacts.ts
    - tests/tls-production-export.test.ts
  </files>
  <action>
    Default `generateTlsProductionArtifacts` to videoOnly: skip narration track generation and omit narration fields from manifest. Bump schemaVersion to 1.1.0; add videoOnly, frameSource. Legacy narration path gated by SECURITY_LAB_INCLUDE_NARRATION=true. Update audio-layer-handoff.md to state deferred for v1.5 video-only. Add test: videoOnly manifest has no narrationTrackPath; trace-hash env yields frameSource ppm-trace-hash.
  </action>
  <acceptance_criteria>Manifest v1.1.0 written; narration optional in default export.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts --testNamePattern=videoOnly|manifest</automated>
  </verify>
  <done>Video-only TLS bundle generator operational.</done>
</task>

<task type="auto">
  <name>Task 3: Video-only rubric option</name>
  <read_first>
    - src/verification/tls-production-rubric.ts
  </read_first>
  <files>
    - src/verification/tls-production-rubric.ts
    - src/render/export/generate-tls-production-artifacts.ts
    - tests/tls-production-export.test.ts
  </files>
  <action>
    Extend assertTlsProductionRubric and buildTlsSecuritySignoff with `{ videoOnly?: boolean }`. When videoOnly, skip assertTlsNarrationAlignment; signoff sets narrationAlignmentValid to null and approvedForProduction based on beat module mapping only. Wire videoOnly in generateTlsProductionArtifacts before writing signoff.
  </action>
  <acceptance_criteria>Video-only rubric passes without narration artifacts; legacy narration tests pass with INCLUDE_NARRATION.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts --testNamePattern=rubric|narration</automated>
  </verify>
  <done>Module rubric decoupled from narration for v1.5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI vs local | trace-hash env must still produce valid manifest |
| Legacy v1.4 | INCLUDE_NARRATION preserves narration path for regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | T | Manifest break | mitigate | schemaVersion bump + optional narration |
| T-23-02 | D | Rubric false pass | mitigate | Module mapping unchanged |
</threat_model>

<verification>
- [ ] `npm run test -- tests/tls-production-export.test.ts`
</verification>

<success_criteria>
- Video-only TLS export with manifest recording renderBackend and frameSource.
</success_criteria>

<output>
After completion, create `.planning/phases/23-tls-3d-production-export/23-01-SUMMARY.md`
</output>
