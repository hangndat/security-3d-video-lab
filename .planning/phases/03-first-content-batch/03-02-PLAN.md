---
phase: 03-first-content-batch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/batch/first-content-batch.ts
  - src/render/remotion/render-composition.tsx
  - tests/first-content-batch.test.ts
  - tests/first-content-batch-export.test.ts
autonomous: true
gap_closure: true
requirements: ["R-03-1", "R-03-3"]
must_haves:
  truths:
    - "Long-form export is assembled from TLS -> SSH -> DNS topic scenes in the real export path."
    - "Long-form transition coherence checks run against the stitched long-form assembly contract."
    - "KPI acceptance fails when retention checkpoints or pacing verdict are null."
    - "KPI acceptance passes only when all required measurable fields are populated."
  artifacts:
    - path: "src/content/batch/first-content-batch.ts"
      provides: "Long-form scene assembly wiring plus KPI non-null acceptance validator."
    - path: "src/render/remotion/render-composition.tsx"
      provides: "Render helper(s) used by tests to build stitched long-form from topic scenes."
    - path: "tests/first-content-batch-export.test.ts"
      provides: "Automated proof long-form is built from TLS/SSH/DNS scene sequence."
    - path: "tests/first-content-batch.test.ts"
      provides: "Automated proof KPI gate rejects null checkpoints and null pacing verdict."
  key_links:
    - from: "longFormAssembly.sequence"
      to: "long-form export input scene"
      via: "assembler function consuming topic scene map in sequence order"
      pattern: "tls,ssh,dns sequence drives long-form render"
    - from: "KPI acceptance validator"
      to: "phase acceptance checks"
      via: "non-null enforcement for p25/p50/p75/completion and pacing verdict"
      pattern: "throws/returns failure on null KPI fields"
---

<objective>
Close phase-03 verification blockers by implementing real stitched long-form assembly in the export path and enforcing measurable KPI completeness at acceptance time.

Purpose: satisfy failed truths in `VERIFICATION.md` without reducing scope, per D-02, D-08, D-17, D-18, D-19, and D-20.
Output: code and tests that prove long-form assembly and KPI non-null gate are operational.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-first-content-batch/03-CONTEXT.md
@.planning/phases/03-first-content-batch/VERIFICATION.md
@.planning/phases/03-first-content-batch/SUMMARY.md
@src/content/batch/first-content-batch.ts
@src/render/remotion/render-composition.tsx
@tests/first-content-batch.test.ts
@tests/first-content-batch-export.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire true TLS->SSH->DNS long-form scene assembly into export path</name>
  <read_first>
    - .planning/phases/03-first-content-batch/VERIFICATION.md
    - src/content/batch/first-content-batch.ts
    - tests/first-content-batch-export.test.ts
    - src/fixtures/golden-scene-spec.json
    - src/fixtures/ssh-scene-spec.json
    - src/fixtures/dns-scene-spec.json
  </read_first>
  <files>src/content/batch/first-content-batch.ts, src/render/remotion/render-composition.tsx, tests/first-content-batch-export.test.ts</files>
  <behavior>
    - Test fails if long-form output uses `goldenSceneSpec` directly instead of an assembled sequence from topic scenes.
    - Test fails if assembled sequence order differs from TLS -> SSH -> DNS.
    - Test fails if transition coherence checks for TLS->SSH and SSH->DNS are not executed.
  </behavior>
  <action>
    Implement a deterministic long-form assembler that consumes `longFormAssembly.sequence` (D-02) and topic scene fixtures, producing one SceneSpec passed to `renderCompositionDemoMp4` for `.artifacts/export/phase03/network-foundations-long-v1.mp4`.

    Add explicit transition coherence validation for `longFormAssembly.transitions` (D-08) before rendering long-form: fail fast if TLS->SSH or SSH->DNS links are missing/misaligned with sequence.

    Update export test to call this stitched assembly path (not direct fixture fallback), and assert long-form render input was derived from the sequence contract.
  </action>
  <acceptance_criteria>
    - Long-form test data source is sequence-driven assembly from TLS, SSH, and DNS scenes (no direct `goldenSceneSpec` long-form fallback).
    - Transition coherence validation runs and passes for TLS->SSH and SSH->DNS.
    - Automated export test passes while proving stitched assembly path is used.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/first-content-batch-export.test.ts -t "exports three shorts plus one long-form artifact"</automated>
  </verify>
  <done>Long-form output path is concretely stitched from TLS->SSH->DNS scenes and verified by deterministic automated test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce measurable KPI acceptance gate with non-null checks</name>
  <read_first>
    - .planning/phases/03-first-content-batch/03-CONTEXT.md
    - .planning/phases/03-first-content-batch/VERIFICATION.md
    - src/content/batch/first-content-batch.ts
    - tests/first-content-batch.test.ts
  </read_first>
  <files>src/content/batch/first-content-batch.ts, tests/first-content-batch.test.ts</files>
  <behavior>
    - KPI acceptance fails when any required retention checkpoint is null.
    - KPI acceptance fails when `pacingVerdict` is null or outside enum.
    - KPI acceptance passes when p25/p50/p75/completion are populated and verdict is valid.
  </behavior>
  <action>
    Add KPI lifecycle helpers in the phase batch module:
    (1) a deterministic populate/update API for KPI checkpoints and verdict,
    (2) a strict acceptance validator that enforces D-17, D-18, D-19, and D-20 non-null completeness.

    Integrate validator into phase acceptance surface (`validateBatchCompleteness` or adjacent acceptance function) so unresolved KPI values block acceptance.

    Replace skeleton-only assertions with measurable gate tests that explicitly fail on null checkpoints/verdict and pass with complete KPI payload.
  </action>
  <acceptance_criteria>
    - Acceptance gate returns/throws failure when any KPI checkpoint or pacing verdict is null.
    - Acceptance gate passes only with complete non-null KPI set and valid pacing verdict enum.
    - Tests assert both failing and passing KPI paths.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/first-content-batch.test.ts -t "KPI"</automated>
  </verify>
  <done>KPI feedback loop is measurable at acceptance time and enforced by automated non-null gate tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression guard for both gap truths in one acceptance command</name>
  <read_first>
    - tests/first-content-batch.test.ts
    - tests/first-content-batch-export.test.ts
    - .planning/phases/03-first-content-batch/VERIFICATION.md
  </read_first>
  <files>tests/first-content-batch.test.ts, tests/first-content-batch-export.test.ts</files>
  <action>
    Harden test naming and assertions so the two failed verification truths are explicitly represented and easy to re-run:
    (a) stitched long-form truth,
    (b) measurable KPI truth.

    Ensure both tests run in CI-friendly deterministic mode and produce actionable failure messages tied to the original gaps.
  </action>
  <acceptance_criteria>
    - A single command executes both gap-closure test suites.
    - Failure output clearly identifies whether the regression is in long-form stitching or KPI measurability.
    - Re-running verification can directly cite these tests as evidence.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/first-content-batch.test.ts tests/first-content-batch-export.test.ts</automated>
  </verify>
  <done>Gap closure remains protected by explicit regression tests that map one-to-one to failed truths in verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content-contract -> export renderer | Untrusted/malformed assembly metadata could produce incorrect or misleading long-form output. |
| KPI payload -> acceptance gate | Null/invalid KPI fields could bypass measurable decision gates if not strictly validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-gap-01 | T | `longFormAssembly` sequencing | mitigate | Validate sequence order and transition pairs before render; fail export on mismatch. |
| T-03-gap-02 | I | KPI acceptance validator | mitigate | Enforce strict non-null checkpoint and enum validation before batch acceptance. |
| T-03-gap-03 | R | gap regression tests | mitigate | Add explicit tests proving stitched source and KPI completeness with deterministic commands. |
</threat_model>

<verification>
- `npm test -- tests/first-content-batch-export.test.ts -t "exports three shorts plus one long-form artifact"`
- `npm test -- tests/first-content-batch.test.ts -t "KPI"`
- `npm test -- tests/first-content-batch.test.ts tests/first-content-batch-export.test.ts`
</verification>

<success_criteria>
- Both failed truths in `.planning/phases/03-first-content-batch/VERIFICATION.md` are covered by passing automated tests.
- Long-form artifact generation path is assembled from TLS->SSH->DNS scenes (not fixture shortcut).
- KPI acceptance gate blocks null checkpoints/verdict and passes on complete measurable payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-content-batch/03-02-SUMMARY.md`.
</output>
