---
phase: 21-headless-capture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/render/headless/capture-viz-frame-png.ts
  - src/render/headless/resolve-production-render-backend.ts
  - src/render/headless/build-viz-three-scene.ts
  - tests/headless-capture.test.ts
autonomous: true
requirements:
  - RENDER-01
must_haves:
  truths:
    - "src/render/headless/ contains capture, resolver, and scene builder modules."
    - "resolveProductionRenderBackend returns trace-hash when SECURITY_LAB_RENDER_BACKEND=trace-hash."
    - "resolveProductionRenderBackend returns r3f-headless when env unset."
    - "captureVizFramePng returns non-empty PNG buffer for golden-scene-spec frame 0."
    - "Repeated captureVizFramePng calls produce identical SHA256 hash on same platform."
  artifacts:
    - path: "src/render/headless/capture-viz-frame-png.ts"
      provides: "PNG capture via @headless-three/renderer."
    - path: "src/render/headless/resolve-production-render-backend.ts"
      provides: "Env-gated backend selection."
    - path: "src/render/headless/build-viz-three-scene.ts"
      provides: "Minimal Three.js scene from compose plan."
    - path: "tests/headless-capture.test.ts"
      provides: "RENDER-01 resolver and capture tests."
  key_links:
    - from: "capture-viz-frame-png.ts"
      to: "src/client/viz/compose-scene.tsx"
      via: "getComposePlan in buildVizThreeScene"
      pattern: "getComposePlan"
    - from: "build-viz-three-scene.ts"
      to: "src/client/viz/style-tokens.ts"
      via: "Token-driven colors and camera FOV"
      pattern: "STYLE_TOKENS"
    - from: "capture-viz-frame-png.ts"
      to: "@headless-three/renderer"
      via: "renderHeadlessPng"
      pattern: "renderHeadlessPng"
---

<objective>
Deliver RENDER-01 foundation: restore headless Three.js capture modules with minimal scene builder and deterministic PNG output.

Purpose: Unblock broken imports in render-composition.tsx and prove headless GL capture before scene parity (22) and TLS 3D export (23).
Output: `src/render/headless/` trio, headless capture tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-headless-capture-foundation/21-CONTEXT.md
@.planning/phases/21-headless-capture-foundation/21-RESEARCH.md
@src/client/viz/compose-scene.tsx
@src/client/viz/style-tokens.ts
@src/fixtures/golden-scene-spec.json
@src/fixtures/tls-production-scene-spec.json
@src/render/remotion/render-composition.tsx
@package.json

<interfaces>
Backend resolver:
```typescript
export type ProductionRenderBackend = "r3f-headless" | "trace-hash";

export function resolveProductionRenderBackend(
  env?: NodeJS.ProcessEnv
): ProductionRenderBackend;
```

Capture API:
```typescript
export type CaptureVizFramePngOptions = {
  width?: number;
  height?: number;
  captionMap?: CaptionTimingMap;
};

export function captureVizFramePng(
  sceneSpec: SceneSpec,
  frame: number,
  options?: CaptureVizFramePngOptions
): Buffer;
```

Scene builder:
```typescript
export type VizThreeSceneBundle = {
  scene: THREE.Scene;
  camera: THREE.PerspectiveCamera;
  plan: ComposePlan;
};

export function buildVizThreeScene(
  sceneSpec: SceneSpec,
  frame: number,
  width: number,
  height: number,
  captionMap?: CaptionTimingMap
): VizThreeSceneBundle;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend resolver and headless module scaffold</name>
  <read_first>
    - .planning/phases/21-headless-capture-foundation/21-RESEARCH.md
    - src/render/remotion/render-composition.tsx
  </read_first>
  <files>
    - src/render/headless/resolve-production-render-backend.ts
  </files>
  <action>
    Create resolve-production-render-backend.ts exporting ProductionRenderBackend and resolveProductionRenderBackend. Map SECURITY_LAB_RENDER_BACKEND: `trace-hash`|`hash` → trace-hash; otherwise r3f-headless. No side effects; pure env parse.
  </action>
  <acceptance_criteria>Resolver is importable from render-composition.tsx without circular deps.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/headless-capture.test.ts --testNamePattern="resolveProductionRenderBackend"</automated>
  </verify>
  <done>Backend selection module ready.</done>
</task>

<task type="auto">
  <name>Task 2: Minimal Three.js scene builder and PNG capture</name>
  <read_first>
    - src/client/viz/compose-scene.tsx
    - src/client/viz/style-tokens.ts
    - docs/style-bible.md
  </read_first>
  <files>
    - src/render/headless/build-viz-three-scene.ts
    - src/render/headless/capture-viz-frame-png.ts
  </files>
  <action>
    Implement buildVizThreeScene: Scene background from STYLE_TOKENS.colorBgDeep, PerspectiveCamera from STYLE_TOKENS.cameraFovIntimate, ambient + directional light. For renderOrder modules: viz-packet-* → sphere at packet position with token color; viz-tunnel-secure → solid torus; viz-tunnel-handshake → wireframe torus. Cert/HUD modules: no mesh in Phase 21 (document skip).
    Implement captureVizFramePng: call buildVizThreeScene, then @headless-three/renderer `render(scene, camera, { width, height, background })`, return Buffer.
  </action>
  <acceptance_criteria>PNG buffer starts with PNG magic bytes; size &gt; 44 bytes.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/headless-capture.test.ts --testNamePattern="captureVizFramePng"</automated>
  </verify>
  <done>Headless capture produces PNG from compose plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RENDER-01 headless capture tests</name>
  <read_first>
    - tests/viz-packet-tunnel-modules.test.ts
  </read_first>
  <files>
    - tests/headless-capture.test.ts
  </files>
  <behavior>
    - Test 1: resolveProductionRenderBackend({}) returns r3f-headless.
    - Test 2: resolveProductionRenderBackend({ SECURITY_LAB_RENDER_BACKEND: "trace-hash" }) returns trace-hash.
    - Test 3: captureVizFramePng(golden-scene-spec, 0) returns PNG magic header.
    - Test 4: two captures same frame produce identical sha256 hash.
    - Test 5: captureVizFramePng(tls-production-scene-spec, 75) returns non-empty buffer when GL available.
  </behavior>
  <action>
    Create tests/headless-capture.test.ts. Use describe.skipIf or try/catch when @headless-three/renderer native binary unavailable so CI with trace-hash-only env does not fail on GL tests. Resolver tests always run.
  </action>
  <acceptance_criteria>Resolver tests pass in CI; GL tests skip gracefully when binary missing.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/headless-capture.test.ts</automated>
  </verify>
  <done>RENDER-01 unit tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native binary → capture | Platform-specific @headless-three/renderer dep |
| CI → GL | trace-hash fallback; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | D | CI non-determinism from GL | mitigate | trace-hash CI path; hash golden local only |
| T-21-02 | E | Missing headless files break render | mitigate | 21-01 restores modules first |
</threat_model>

<verification>
- [ ] `npm run test -- tests/headless-capture.test.ts`
- [ ] Imports in render-composition.tsx resolve without error
</verification>

<success_criteria>
- RENDER-01: Headless PNG capture module exists with env-gated backend resolver and deterministic capture tests.
</success_criteria>

<output>
After completion, create `.planning/phases/21-headless-capture-foundation/21-01-SUMMARY.md`
</output>
