---
phase: 21-headless-capture-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 21-01
files_modified:
  - src/render/remotion/render-composition.tsx
  - src/render/export/generate-tls-production-artifacts.ts
  - scripts/verify-headless-capture.mjs
  - tests/render-composition.test.ts
  - tests/headless-capture.test.ts
  - tests/tls-production-export.test.ts
  - .github/workflows/ci.yml
  - package.json
  - docs/tls-crew-walkthrough.md
autonomous: true
requirements:
  - RENDER-01
must_haves:
  truths:
    - "renderCompositionProductionMp4 r3f-headless path encodes PNG frames via ffmpeg."
    - "renderCompositionProductionMp4 trace-hash path unchanged for CI compatibility."
    - "CI sets SECURITY_LAB_RENDER_BACKEND=trace-hash for PR validation."
    - "verify-headless-capture.mjs writes phase21/headless-capture.json with gateStatus pass."
    - "render-composition.test.ts passes with restored headless imports."
  artifacts:
    - path: "scripts/verify-headless-capture.mjs"
      provides: "RENDER-01 verification gate."
    - path: ".artifacts/verification/phase21/headless-capture.json"
      provides: "Machine-readable Phase 21 evidence."
  key_links:
    - from: "render-composition.tsx"
      to: "src/render/headless/capture-viz-frame-png.ts"
      via: "renderProductionFramesR3fHeadless loop"
      pattern: "captureVizFramePng"
    - from: "verify-headless-capture.mjs"
      to: "tests/headless-capture.test.ts"
      via: "Gate orchestrator"
      pattern: "headless-capture"
    - from: ".github/workflows/ci.yml"
      to: "resolve-production-render-backend.ts"
      via: "SECURITY_LAB_RENDER_BACKEND env"
      pattern: "trace-hash"
---

<objective>
Deliver RENDER-01 integration: wire headless capture into production MP4 path, CI backend policy, and verification gate.

Purpose: End-to-end proof that backend switching works and Phase 21 is machine-verifiable before Phase 22 scene parity.
Output: CI env, verify script, integration tests, updated docs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-headless-capture-foundation/21-CONTEXT.md
@.planning/phases/21-headless-capture-foundation/21-RESEARCH.md
@src/render/remotion/render-composition.tsx
@scripts/verify-tls-production.mjs
@.github/workflows/ci.yml

<interfaces>
Production render options (existing):
```typescript
export type RenderCompositionProductionOptions = {
  profile?: Partial<ProductionRenderProfile>;
  captionMap?: CaptionTimingMap;
  backend?: ProductionRenderBackend;
};
```

Verify JSON shape:
```typescript
{
  generatedAt: string;
  gateStatus: "pass" | "fail";
  backendPolicy: { ci: "trace-hash"; localDefault: "r3f-headless" };
  blockingCriteria: { resolverTestsPassed: boolean; renderCompositionTestsPassed: boolean };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production render path integration</name>
  <read_first>
    - src/render/remotion/render-composition.tsx
    - src/render/headless/capture-viz-frame-png.ts
  </read_first>
  <files>
    - src/render/remotion/render-composition.tsx
    - tests/render-composition.test.ts
  </files>
  <action>
    Verify renderCompositionProductionMp4 uses resolveProductionRenderBackend and captureVizFramePng correctly. Fix any import/type issues. Add test: with backend trace-hash, short production render produces MP4; with backend r3f-headless (skipIf no GL), single-frame or 2-frame TLS fixture produces non-empty MP4. Keep renderCompositionDemoMp4 unchanged.
  </action>
  <acceptance_criteria>render-composition tests pass; trace-hash backend always works in CI.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/render-composition.test.ts</automated>
  </verify>
  <done>Production renderer wired to headless modules.</done>
</task>

<task type="auto">
  <name>Task 2: CI backend policy and documentation</name>
  <read_first>
    - .github/workflows/ci.yml
    - docs/tls-crew-walkthrough.md
  </read_first>
  <files>
    - .github/workflows/ci.yml
    - docs/tls-crew-walkthrough.md
    - src/render/export/generate-tls-production-artifacts.ts
  </files>
  <action>
    Set SECURITY_LAB_RENDER_BACKEND=trace-hash on pr-full-validation job env (or render smoke steps). Document r3f-headless local default and env override in tls-crew-walkthrough Step 5 render handoff. Optionally log resolved backend in generateTlsProductionArtifacts manifest field renderBackend (non-breaking additive field).
  </action>
  <acceptance_criteria>CI does not require headless GL; docs mention both backends.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts --testNamePattern="production"</automated>
  </verify>
  <done>CI and docs reflect backend policy.</done>
</task>

<task type="auto">
  <name>Task 3: verify-headless-capture gate and npm script</name>
  <read_first>
    - scripts/verify-tls-production.mjs
    - scripts/verify-tts-integration.mjs
  </read_first>
  <files>
    - scripts/verify-headless-capture.mjs
    - package.json
    - tests/headless-capture.test.ts
  </files>
  <action>
    Create verify-headless-capture.mjs: run headless-capture + render-composition tests with SECURITY_LAB_RENDER_BACKEND=trace-hash for CI-safe subset; write .artifacts/verification/phase21/headless-capture.json. Add npm script verify:headless-capture. Gate passes when resolver tests pass and trace-hash render smoke passes.
  </action>
  <acceptance_criteria>node scripts/verify-headless-capture.mjs --quick exits 0.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/headless-capture.test.ts && node scripts/verify-headless-capture.mjs --quick</automated>
  </verify>
  <done>RENDER-01 verification gate operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → headless GL | CI never invokes r3f-headless without skip guards |
| verify script → tests | trace-hash env forced for gate runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | D | CI GL flake | mitigate | trace-hash in ci.yml |
| T-21-04 | T | Broken render imports | mitigate | Task 1 integration tests |
</threat_model>

<verification>
- [ ] `node scripts/verify-headless-capture.mjs --quick`
- [ ] `npm run test -- tests/render-composition.test.ts`
- [ ] `npm run test -- tests/headless-capture.test.ts`
</verification>

<success_criteria>
- RENDER-01: Headless capture integrated, CI-safe, machine-verifiable via phase21 JSON gate.
</success_criteria>

<output>
After completion, create `.planning/phases/21-headless-capture-foundation/21-02-SUMMARY.md`
</output>
