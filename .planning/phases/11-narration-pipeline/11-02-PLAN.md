---
phase: 11-narration-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/render/export/build-long-form-export-bundle.ts
  - tests/narration-export.test.ts
  - scripts/verify-narration-pipeline.mjs
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - VOIC-01
  - VOIC-02
must_haves:
  truths:
    - "Long-form export bundle links caption timing map and narration track manifest."
    - "Export artifact paths follow stable slug[:branchId] naming conventions."
    - "Bundle content hashes are identical across two generation runs."
    - "verify-narration-pipeline.mjs --quick passes in CI."
  artifacts:
    - path: "src/render/export/build-long-form-export-bundle.ts"
      provides: "Unified export bundle builder with caption + narration linkage."
    - path: "scripts/verify-narration-pipeline.mjs"
      provides: "Phase 11 verification gate and evidence writer."
    - path: "tests/narration-export.test.ts"
      provides: "Export bundle determinism and artifact path tests."
  key_links:
    - from: "build-long-form-export-bundle.ts"
      to: "generate-narration-track.ts"
      via: "Embeds narration track alongside caption map in bundle"
      pattern: "narrationTrack"
    - from: "build-long-form-export-bundle.ts"
      to: "fingerprint.ts"
      via: "Optional deterministic manifest when trace inputs provided"
      pattern: "buildDeterministicManifest"
---

<objective>
Attach synchronized narration track metadata to long-form export artifacts and add Phase 11 CI verification.

Purpose: Close VOIC-02 with export bundle linkage and deterministic artifact naming; validate end-to-end pipeline in quick verify mode.
Output: Export bundle builder, export tests, verify script, and package.json script entry.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-narration-pipeline/11-CONTEXT.md
@.planning/phases/11-narration-pipeline/11-01-PLAN.md
@src/render/export/fingerprint.ts
@scripts/verify-narrative-composition.mjs

<interfaces>
Export bundle (D-11, D-12):
```typescript
interface LongFormExportBundle {
  schemaVersion: "1.0.0";
  assemblySlug: string;
  branchId?: string;
  sceneId: string;
  captionTimingMapPath: string;
  narrationTrackPath: string;
  bundlePath: string;
  captionMapHash: string;
  narrationTrackHash: string;
  bundleHash: string;
  narrationTrack: NarrationTrackManifest;
  captionTimingMap: CaptionTimingMap;
}

buildLongFormExportBundle(
  assemblySlug: string,
  topicScenes: Partial<Record<TopicId, SceneSpec>>,
  options?: { branchId?: string; traceInputs?: string[] }
): LongFormExportBundle
```

Artifact paths (D-12):
- Linear: `.artifacts/exports/network-foundations-long-v1/export-bundle.json`
- Branched: `.artifacts/exports/content-depth-branched-v1-defense-path/export-bundle.json`

Verify script pattern: mirror `verify-narrative-composition.mjs` with JSON + VERIFICATION.md evidence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Long-form export bundle builder</name>
  <read_first>
    - src/render/export/fingerprint.ts
    - src/content/narration/generate-narration-track.ts
    - src/content/composition/generate-caption-timing-map.ts
  </read_first>
  <files>
    - src/render/export/build-long-form-export-bundle.ts
    - tests/narration-export.test.ts
  </files>
  <behavior>
    - Test 1: buildLongFormExportBundle for network-foundations-long-v1 includes caption + narration with matching beat counts.
    - Test 2: bundleHash identical on two runs with same inputs.
    - Test 3: branched defense-path bundle sceneId matches stitch convention.
    - Test 4: bundle JSON validates required path fields and content hashes.
  </behavior>
  <action>Implement buildLongFormExportBundle orchestrating generateCaptionTimingMap, generateNarrationTrack (stub provider), validateNarrationAlignment, and artifact writes. Compute stable hashes for caption map, narration track, and bundle envelope. Include sceneId from assembly slug + optional branchId. writeExportBundleArtifacts(root, bundle) writes caption, narration WAV dir, and export-bundle.json.</action>
  <acceptance_criteria>VOIC-02: export bundle links caption timing map and narration track with stable artifact paths.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/narration-export.test.ts</automated>
  </verify>
  <done>Export bundle builder delivers synchronized narration metadata alongside captions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Optional deterministic manifest linkage</name>
  <read_first>
    - src/render/export/fingerprint.ts
    - tests/e2e-canonical-flows.test.ts
  </read_first>
  <files>
    - src/render/export/build-long-form-export-bundle.ts
    - tests/narration-export.test.ts
  </files>
  <behavior>
    - Test 1: when traceInputs provided, bundle includes deterministicManifest with sceneId.
    - Test 2: narrationTrackHash stable when traceInputs differ but caption/narration inputs same.
  </behavior>
  <action>Extend export bundle with optional deterministicManifest field when caller passes traceInputs + spec metadata. Keep narration/caption hashes independent of video trace (VOIC-02 scope is metadata linkage, not video mux).</action>
  <acceptance_criteria>Export bundle can optionally embed existing deterministic manifest without breaking narration hash stability.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/narration-export.test.ts --testNamePattern="manifest|trace"</automated>
  </verify>
  <done>Export bundle integrates with existing fingerprint infrastructure.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 11 verify script and CI hook</name>
  <read_first>
    - scripts/verify-narrative-composition.mjs
    - .github/workflows/ci.yml
  </read_first>
  <files>
    - scripts/verify-narration-pipeline.mjs
    - package.json
    - .github/workflows/ci.yml
  </files>
  <behavior>
    - Script quick mode runs narration-track + narration-export tests and exits 0.
    - package.json exposes verify:narration-pipeline script.
    - CI pr-validation job invokes verify:narration-pipeline --quick.
  </behavior>
  <action>Create verify-narration-pipeline.mjs with suite runner, assembly coverage summary, JSON artifact at .artifacts/verification/phase11/narration-pipeline.json, and VERIFICATION.md writer. Add npm script. Add CI step after verify:narrative-composition in pr-validation job.</action>
  <acceptance_criteria>Phase 11 quick verify gate green; CI invokes narration pipeline validation on PRs.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-narration-pipeline.mjs --quick</automated>
  </verify>
  <done>Pipeline runs deterministically in CI with stable artifact naming (roadmap success criterion 3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export bundle → artifacts | Hashes verify caption/narration integrity at rest |
| CI verify → tests | Quick mode avoids e2e video render dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | T | export-bundle.json | mitigate | bundleHash covers caption + narration content hashes. |
| T-11-05 | I | artifact paths | mitigate | slug[:branchId] namespace prevents cross-assembly overwrite. |
</threat_model>

<verification>
- [ ] `npm run test -- tests/narration-track.test.ts tests/narration-export.test.ts`
- [ ] `node scripts/verify-narration-pipeline.mjs --quick`
</verification>

<success_criteria>
- VOIC-02: Long-form exports include narration track metadata linked to caption beats.
- Deterministic bundle hashes across repeated runs.
- CI quick verify gate for phase 11.
</success_criteria>

<output>
After completion, create `.planning/phases/11-narration-pipeline/11-02-SUMMARY.md`
</output>
