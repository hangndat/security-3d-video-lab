---
phase: 02-mvp-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - package.json
  - src/render/remotion/render-composition.tsx
  - src/render/export/fingerprint.ts
  - tests/render-repro-smoke.test.ts
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - User can export a deterministic demo MP4 where the output originates from Remotion composition frames (not synthetic FFmpeg sources).
    - User can compute and assert the exported demo artifact fingerprint from actual rendered frame data plus normalized metadata.
  artifacts:
    - path: package.json
      provides: export pipeline scripts wired to Remotion-driven render output and fingerprint verification
    - path: src/render/remotion/render-composition.tsx
      provides: executable frame-state source consumed by export pipeline
    - path: src/render/export/fingerprint.ts
      provides: reusable fingerprint generation path applied to exported demo artifact inputs
    - path: tests/render-repro-smoke.test.ts
      provides: automated assertion that export pipeline uses composition-derived frame hashes
  key_links:
    - from: package.json
      to: src/render/remotion/render-composition.tsx
      via: demo export command invokes composition-backed render path
      pattern: "export:demo|render:demo"
    - from: tests/render-repro-smoke.test.ts
      to: src/render/export/fingerprint.ts
      via: exported-output fingerprint is built and validated from real frame hash inputs
      pattern: "computeOutputFingerprint|buildDeterministicManifest"
---

<objective>
Close the remaining Phase 02 verification gap by wiring the real deterministic composition to MP4 export and tying fingerprint checks to that concrete output path.

Purpose: Ensure the MVP render/export truth is achieved end-to-end, not by disconnected synthetic artifact generation.
Output: A composition-backed export flow and reproducibility assertions that operate on real rendered output data.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-engine/02-VERIFICATION.md
@.planning/phases/02-mvp-engine/02-03-PLAN.md
@.planning/phases/02-mvp-engine/02-03-SUMMARY.md
@.planning/phases/02-mvp-engine/02-CONTEXT.md
@src/render/remotion/render-composition.tsx
@src/render/export/fingerprint.ts
@package.json
</context>

<source_audit>
- GOAL: COVERED - phase truth 8 failure ("deterministic demo sequence to MP4 through Remotion and FFmpeg") is directly implemented.
- REQ: COVERED - requirements input is null for this gap-closure request; no additional requirement IDs provided.
- RESEARCH: COVERED - Remotion + FFmpeg deterministic export and fingerprint policy from `02-RESEARCH.md` is applied.
- CONTEXT: COVERED - D-04, D-14, D-29, D-31, D-32 are enforced in export wiring and reproducibility checks; no deferred idea is included.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire demo export to composition-backed render path</name>
  <read_first>
    - .planning/phases/02-mvp-engine/02-VERIFICATION.md
    - .planning/phases/02-mvp-engine/02-03-SUMMARY.md
    - src/render/remotion/render-composition.tsx
    - package.json
  </read_first>
  <files>package.json, src/render/remotion/render-composition.tsx</files>
  <behavior>
    - Test 1: `export:demo` fails if composition render output is unavailable.
    - Test 2: `export:demo` succeeds only when MP4 is produced from Remotion composition-derived frames.
  </behavior>
  <action>Replace the synthetic FFmpeg `lavfi color` export path with a deterministic composition-driven flow per D-04 and D-14. Keep render state derivation pure and sourced from `SceneSpec` + frame index, then route generated composition output into FFmpeg encoding for the demo artifact. Preserve script ergonomics so this remains executable via `npm run export:demo` in CI/local runs.</action>
  <verify>
    <automated>npm run export:demo</automated>
  </verify>
  <acceptance_criteria>The produced `.artifacts/export/demo.mp4` is generated from Remotion composition output and not from synthetic FFmpeg generator inputs.</acceptance_criteria>
  <done>Demo export command is wired to deterministic composition output path end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind fingerprint assertions to exported demo output path</name>
  <read_first>
    - .planning/phases/02-mvp-engine/02-VERIFICATION.md
    - src/render/export/fingerprint.ts
    - tests/render-repro-smoke.test.ts
  </read_first>
  <files>src/render/export/fingerprint.ts, tests/render-repro-smoke.test.ts, package.json</files>
  <behavior>
    - Test 1: reproducibility smoke test computes manifest/fingerprint from composition-derived frame hashes used in export path.
    - Test 2: identical export inputs produce identical fingerprints.
    - Test 3: changed frame hash or normalized metadata produces mismatch and diff-bundle failure.
  </behavior>
  <action>Wire fingerprint computation for the demo export path so manifest and output fingerprint are built from actual rendered frame hashes plus normalized metadata per D-29 and D-31. Update reproducibility smoke coverage to assert that the export pipeline data path is the source of fingerprint inputs, and preserve hard-fail diff bundle behavior per D-32.</action>
  <verify>
    <automated>npm run test:e2e:render-smoke</automated>
  </verify>
  <acceptance_criteria>Reproducibility checks for the demo export use real composition-derived frame data and fail hard with diff evidence on mismatches.</acceptance_criteria>
  <done>Fingerprint validation is connected to the same concrete export path used to produce the demo MP4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composition render output -> ffmpeg encoding | Untrusted or synthetic substitution can bypass deterministic content guarantees |
| export fingerprint inputs -> reproducibility decision | Incorrect input source can produce false confidence in reproducibility status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | `package.json` export scripts | mitigate | Remove synthetic generator path and enforce composition-backed export invocation |
| T-02-11 | Repudiation | `tests/render-repro-smoke.test.ts` | mitigate | Assert fingerprint inputs are composition-derived and fail with diff bundles on mismatch |
| T-02-12 | Information Disclosure | `.artifacts/repro` bundles | accept | Bundles contain deterministic hashes/metadata only; no sensitive data in scope |
</threat_model>

<verification>
- `npm run export:demo`
- `npm run test:e2e:render-smoke`
- `npm test`
</verification>

<success_criteria>
- Verification truth 8 is now satisfied by an actual composition-backed MP4 export path.
- Export reproducibility evidence is computed from the same rendered output path being shipped.
- Phase 02 can be re-verified without disconnected synthetic export behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mvp-engine/02-04-SUMMARY.md`
</output>
