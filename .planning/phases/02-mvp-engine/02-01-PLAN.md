---
phase: 02-mvp-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/engine/contracts/scene-spec.ts
  - src/engine/contracts/capability-registry.ts
  - src/engine/contracts/validation-errors.ts
  - src/engine/contracts/validate-scene-spec.ts
  - src/fixtures/golden-scene-spec.json
  - tests/capability-registry.test.ts
  - tests/scene-spec-validation.test.ts
autonomous: true
requirements:
  - P02-ENGINE-DETERMINISTIC-SEQUENCE
  - P02-STRICT-SCENE-CONTRACT
must_haves:
  truths:
    - User can validate a SceneSpec before rendering and get deterministic pass/fail results.
    - Invalid SceneSpec inputs fail with structured errors that identify exact failing paths.
    - User can extend post-MVP capability switches only through an explicit feature-flag capability registry.
    - CI has a golden SceneSpec fixture that proves schema conformance in automated checks.
  artifacts:
    - path: package.json
      provides: Node scripts and dependencies for deterministic engine/test execution
    - path: src/engine/contracts/scene-spec.ts
      provides: Strict SceneSpec schema with explicit schemaVersion and numeric bounds
    - path: src/engine/contracts/capability-registry.ts
      provides: Explicit feature-flag capability registry and allowlist for post-MVP extension points
    - path: src/engine/contracts/validate-scene-spec.ts
      provides: Pre-render validation entrypoint with strict unknown-field rejection
    - path: src/engine/contracts/validation-errors.ts
      provides: Structured validation error shape (path, code, message, hint)
    - path: src/fixtures/golden-scene-spec.json
      provides: Canonical valid fixture for CI gate checks
    - path: tests/capability-registry.test.ts
      provides: Automated tests proving unknown/disabled capabilities fail deterministic validation
    - path: tests/scene-spec-validation.test.ts
      provides: Automated tests for strict contract behavior and deterministic validation
  key_links:
    - from: src/engine/contracts/validate-scene-spec.ts
      to: src/engine/contracts/scene-spec.ts
      via: strict schema parse and schemaVersion gate
      pattern: "schemaVersion|strict|parse"
    - from: src/engine/contracts/validate-scene-spec.ts
      to: src/engine/contracts/capability-registry.ts
      via: capability key allowlist gate for feature-flagged post-MVP extensions
      pattern: "capability|featureFlag|registry"
    - from: tests/scene-spec-validation.test.ts
      to: src/fixtures/golden-scene-spec.json
      via: golden fixture load and validation assertion
      pattern: "golden-scene-spec|validateSceneSpec"
---

<objective>
Establish the contract-first deterministic foundation for Phase 02.

Purpose: Ensure every downstream timeline/camera/packet/render operation is gated by strict, reproducible SceneSpec validation per D-01..D-12.
Output: Bootstrapped TypeScript workspace, strict SceneSpec contract modules, golden fixture, and automated validation tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-engine/02-CONTEXT.md
@.planning/phases/02-mvp-engine/02-RESEARCH.md
@.planning/phases/02-mvp-engine/02-PATTERNS.md
@.planning/adr/ADR-001-tech-stack.md
@.planning/adr/ADR-002-timeline-architecture.md
@docs/roadmap.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap deterministic engine workspace</name>
  <read_first>
    - .planning/phases/02-mvp-engine/02-RESEARCH.md
    - .planning/adr/ADR-001-tech-stack.md
  </read_first>
  <files>package.json, tsconfig.json</files>
  <behavior>
    - Test 1: `npm run test:quick -- scene-spec-validation` executes successfully with no missing script/config errors.
    - Test 2: TypeScript strict mode rejects implicit any in engine modules.
  </behavior>
  <action>Create `package.json` with pinned dependencies (`three`, `@react-three/fiber`, `@react-three/drei`, `remotion`, `zod`, `ajv`, `seedrandom`) and scripts `test:quick`, `test`, `render:demo`, `export:demo` per D-30. Create `tsconfig.json` with strict compiler settings, NodeNext module resolution, and deterministic-friendly noImplicitAny/noUncheckedIndexedAccess enabled. Pattern analog: follow `package.json` / `tsconfig.json` config mapping in `.planning/phases/02-mvp-engine/02-PATTERNS.md` (closest analog `.planning/config.json`).</action>
  <verify>
    <automated>npm install && npm run test:quick -- scene-spec-validation</automated>
  </verify>
  <acceptance_criteria>Dependency installation succeeds, all required scripts exist, and test command resolves without configuration errors.</acceptance_criteria>
  <done>Workspace is executable for contract-first development and deterministic validation tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict SceneSpec contract and validator gate</name>
  <read_first>
    - .planning/phases/02-mvp-engine/02-CONTEXT.md
    - .planning/phases/02-mvp-engine/02-PATTERNS.md
  </read_first>
  <files>src/engine/contracts/scene-spec.ts, src/engine/contracts/capability-registry.ts, src/engine/contracts/validation-errors.ts, src/engine/contracts/validate-scene-spec.ts, tests/capability-registry.test.ts</files>
  <behavior>
    - Test 1: Unknown fields fail validation with structured error objects.
    - Test 2: Unsupported `schemaVersion` values hard-fail with explicit guidance.
    - Test 3: Missing deterministic-critical fields (seed, IDs, frame constraints) block validation.
    - Test 4: Unregistered or disabled capability flags fail with structured errors from the capability registry gate.
  </behavior>
  <action>Implement strict `SceneSpec` schema and parse gate per D-01, D-02, D-05, D-06, D-10, D-11, D-12 using `z.strictObject`. Enforce author-defined stable IDs across scene/track/cue/packet actors (D-10) and return structured errors with `path`, `code`, `message`, `hint` (D-07). Add explicit feature-flag capability registry wiring per D-09 in `capability-registry.ts` and gate capability declarations in `validateSceneSpec()` with no implicit fallback entries. Pattern analogs: follow strict contract/error surfaces from `.planning/phases/02-mvp-engine/02-PATTERNS.md` entries for `src/engine/contracts/scene-spec.ts`, `src/engine/contracts/validation-errors.ts`, and deterministic scheduling constraints from ADR-backed mappings.</action>
  <verify>
    <automated>npm run test:quick -- scene-spec-validation</automated>
  </verify>
  <acceptance_criteria>Validator rejects unknown keys and unsupported schemaVersion, emits structured errors, and fails when deterministic-critical fields are absent.</acceptance_criteria>
  <done>All SceneSpec inputs are deterministically validated before any render/export entrypoint can proceed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add golden SceneSpec fixture and validation tests</name>
  <read_first>
    - .planning/phases/02-mvp-engine/02-CONTEXT.md
    - .planning/adr/ADR-003-e2e-testing-strategy.md
  </read_first>
  <files>src/fixtures/golden-scene-spec.json, tests/scene-spec-validation.test.ts</files>
  <behavior>
    - Test 1: Golden fixture validates cleanly and consistently across repeated runs.
    - Test 2: Fixture mutation introducing unknown field fails with D-05 error semantics.
    - Test 3: Fixture mutation removing seed fails deterministic-critical gate per D-11/D-13 linkage.
  </behavior>
  <action>Create canonical `golden-scene-spec.json` for CI per D-08, including deterministic seed, stable IDs, and explicit capability declarations compatible with D-09 registry policy. Implement test coverage for pass/fail contract behavior and stable error envelopes. Ensure tests use deterministic fixtures only and avoid runtime randomness. Pattern analog: follow test/fixture mappings in `.planning/phases/02-mvp-engine/02-PATTERNS.md` for `src/fixtures/golden-scene-spec.json` and `tests/scene-spec-validation.test.ts`.</action>
  <verify>
    <automated>npm run test:quick -- scene-spec-validation</automated>
  </verify>
  <acceptance_criteria>Golden fixture is committed and tests prove strict contract enforcement and deterministic validation outputs.</acceptance_criteria>
  <done>CI-ready fixture and contract tests are in place for downstream engine modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-provided SceneSpec -> engine validator | Untrusted spec data crosses into deterministic runtime contracts |
| test fixtures -> CI gate | Fixture integrity affects deterministic correctness guarantees |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `src/engine/contracts/validate-scene-spec.ts` | mitigate | Enforce strict schema and unknown-key rejection per D-02/D-05 before render path |
| T-02-02 | Repudiation | `src/engine/contracts/validation-errors.ts` | mitigate | Emit stable structured errors (`path`,`code`,`message`,`hint`) per D-07 for auditable failures |
| T-02-03 | Denial of Service | `src/engine/contracts/scene-spec.ts` | mitigate | Apply numeric bounds and required fields per D-11/D-12 to fail fast on malformed specs |
</threat_model>

<verification>
- `npm run test:quick -- scene-spec-validation`
- `npm run test`
</verification>

<success_criteria>
- SceneSpec is the single strict contract gate with explicit schemaVersion handling.
- Golden fixture and tests enforce deterministic validation behavior in CI.
- Unknown fields, missing deterministic-critical fields, and unsupported versions hard-fail with structured errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mvp-engine/02-01-SUMMARY.md`
</output>
