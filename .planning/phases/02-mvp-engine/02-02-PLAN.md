---
phase: 02-mvp-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/engine/timeline/scheduler.ts
  - src/engine/camera/preset-registry.ts
  - src/engine/packet/packet-state.ts
  - src/client/packet/packet-interpolator.ts
  - tests/timeline-determinism.test.ts
  - tests/camera-preset-bounds.test.ts
  - tests/packet-engine.integration.test.ts
autonomous: true
requirements:
  - P02-PACKET-FLOWS-REUSABLE
  - P02-CAMERA-CINEMATIC-CONTROL
must_haves:
  truths:
    - User can run deterministic frame-index timeline scheduling from the same SceneSpec seed and get identical state traces.
    - User can author camera behavior through versioned shot presets with bounded overrides and explicit transitions.
    - User can simulate packet route interpolation, branching, and termination deterministically from declared IDs/events.
  artifacts:
    - path: src/engine/timeline/scheduler.ts
      provides: frame-index deterministic scheduler with stable sort and cue resolution
    - path: src/engine/camera/preset-registry.ts
      provides: versioned camera preset registry with bounded override validation
    - path: src/engine/packet/packet-state.ts
      provides: deterministic packet route/spawn/lifecycle state model consumed by render clients
    - path: src/client/packet/packet-interpolator.ts
      provides: browser/client packet interpolation implementation over deterministic route state
    - path: tests/timeline-determinism.test.ts
      provides: scheduler determinism tests for same seed/frame inputs
    - path: tests/camera-preset-bounds.test.ts
      provides: bounds and transition-cue enforcement tests
    - path: tests/packet-engine.integration.test.ts
      provides: packet route, branching, and terminal-state integration coverage
  key_links:
    - from: src/engine/timeline/scheduler.ts
      to: src/engine/camera/preset-registry.ts
      via: timeline cue resolution drives camera shot transitions
      pattern: "startFrame|track|transition|preset"
    - from: src/engine/timeline/scheduler.ts
      to: src/engine/packet/packet-state.ts
      via: packet events are applied per frame-index timeline windows for deterministic state snapshots
      pattern: "frame|packet|spawn|terminal"
    - from: src/client/packet/packet-interpolator.ts
      to: src/engine/packet/packet-state.ts
      via: browser interpolation consumes deterministic route nodes and lifecycle states
      pattern: "interpolate|route|terminal|state"
---

<objective>
Implement deterministic engine behavior modules on top of the strict SceneSpec contract.

Purpose: Convert validated SceneSpec data into deterministic timeline, camera, and packet state transitions that can be reused for MVP protocol cinematics.
Output: Scheduler, camera preset registry, and packet state/interpolator modules with deterministic automated tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-engine/02-CONTEXT.md
@.planning/phases/02-mvp-engine/02-RESEARCH.md
@.planning/phases/02-mvp-engine/02-PATTERNS.md
@.planning/adr/ADR-002-timeline-architecture.md
@.planning/phases/02-mvp-engine/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic frame-index scheduler</name>
  <read_first>
    - .planning/adr/ADR-002-timeline-architecture.md
    - .planning/phases/02-mvp-engine/02-01-SUMMARY.md
  </read_first>
  <files>src/engine/timeline/scheduler.ts, tests/timeline-determinism.test.ts</files>
  <behavior>
    - Test 1: same SceneSpec + same seed + same frame index yields strict-equal timeline state.
    - Test 2: events sort stably by (`startFrame`,`track`,`id`) and never drift across runs.
    - Test 3: duplicate/overlapping semantics for same event ID fail deterministic validation.
  </behavior>
  <action>Implement timeline scheduler per D-14 and D-15 as pure frame-index function with stable ordering rules from ADR-002. Enforce ID semantics from D-10 and deterministic-critical constraints from D-11. Provide deterministic trace output shape that later render/fingerprint steps can hash. Pattern analog: follow `.planning/phases/02-mvp-engine/02-PATTERNS.md` mapping for `src/engine/timeline/scheduler.ts` (ADR-002 stable sort + pure function state).</action>
  <verify>
    <automated>npm run test:quick -- timeline-determinism</automated>
  </verify>
  <acceptance_criteria>Scheduler output is deterministic for identical inputs and rejects invalid event ordering/id semantics.</acceptance_criteria>
  <done>Deterministic timeline core exists with passing strict determinism tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement camera preset orchestration with bounded overrides</name>
  <read_first>
    - .planning/phases/02-mvp-engine/02-CONTEXT.md
    - .planning/phases/02-mvp-engine/02-PATTERNS.md
  </read_first>
  <files>src/engine/camera/preset-registry.ts, tests/camera-preset-bounds.test.ts</files>
  <behavior>
    - Test 1: timeline-cued shot preset resolution works with explicit transition cues.
    - Test 2: out-of-bounds overrides hard-fail with structured error hints.
    - Test 3: focus/follow references to packet actor IDs/groups resolve deterministically.
  </behavior>
  <action>Create centrally versioned shot preset registry per D-23 with timeline cue application per D-19 and explicit transitions per D-21. Allow bounded overrides only per D-20 and hard-fail out-of-bounds requests per D-24. Validate focus/follow references against known packet actor IDs/groups per D-22. Pattern analog: follow `.planning/phases/02-mvp-engine/02-PATTERNS.md` entry for `src/engine/camera/preset-registry.ts` (context policy D-19..D-24).</action>
  <verify>
    <automated>npm run test:quick -- camera-preset-bounds</automated>
  </verify>
  <acceptance_criteria>Camera presets are applied from timeline cues, invalid overrides fail deterministically, and packet references validate correctly.</acceptance_criteria>
  <done>Reusable, bounded, deterministic camera orchestration is available to the render pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement deterministic packet interpolation, branching, and lifecycle states</name>
  <read_first>
    - .planning/phases/02-mvp-engine/02-CONTEXT.md
    - .planning/adr/ADR-002-timeline-architecture.md
  </read_first>
  <files>src/engine/packet/packet-state.ts, src/client/packet/packet-interpolator.ts, tests/packet-engine.integration.test.ts</files>
  <behavior>
    - Test 1: packet positions interpolate deterministically over declared route segments.
    - Test 2: explicit spawn events produce stable parent/child lineage across repeated runs.
    - Test 3: terminal states apply deterministic visual flags and stop further transitions.
  </behavior>
  <action>Implement packet capability split aligned to the Architectural Responsibility Map: keep deterministic route/spawn/lifecycle state contracts in `src/engine/packet/packet-state.ts` (secondary backend role), and implement path-segment interpolation in `src/client/packet/packet-interpolator.ts` as the primary Browser/Client tier owner per D-25. Model branching via explicit spawn events with parent/child IDs (D-27), lifecycle termination via explicit terminal states (D-28), and deterministic trail template bounds (D-26). Ensure browser interpolation consumes only frame-indexed deterministic state (no wall-clock/device-local state). Pattern analog: follow `.planning/phases/02-mvp-engine/02-PATTERNS.md` entries for both packet modules (`packet-simulator` mapped to ADR-002 + CONTEXT lifecycle policy).</action>
  <verify>
    <automated>npm run test:quick -- packet-engine</automated>
  </verify>
  <acceptance_criteria>Packet interpolation, branching, and terminal-state behavior are deterministic and reproducible from SceneSpec + frame index + seed.</acceptance_criteria>
  <done>Packet simulation primitives are reusable and deterministic for MVP protocol scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated SceneSpec -> deterministic scheduler | Contract-compliant but still untrusted timeline payloads cross into execution logic |
| timeline cues -> camera/packet resolvers | Cross-module references can be tampered or malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | `src/engine/timeline/scheduler.ts` | mitigate | Stable sort and strict event constraints prevent malformed cue execution drift |
| T-02-05 | Elevation of Privilege | `src/engine/camera/preset-registry.ts` | mitigate | Restrict override keys/ranges and hard-fail out-of-bounds values per D-24 |
| T-02-06 | Repudiation | `src/engine/packet/packet-state.ts`, `src/client/packet/packet-interpolator.ts` | mitigate | Preserve deterministic parent/child and terminal state traces and consume them via frame-index client interpolation only |
</threat_model>

<verification>
- `npm run test:quick -- timeline-determinism`
- `npm run test:quick -- camera-preset-bounds`
- `npm run test:quick -- packet-engine`
</verification>

<success_criteria>
- Timeline, camera, and packet subsystems run deterministically on frame-index timebase.
- Camera preset and packet reference semantics enforce strict bounds and IDs.
- Integration tests prove stable repeatable behavior from same spec/seed inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mvp-engine/02-02-SUMMARY.md`
</output>
