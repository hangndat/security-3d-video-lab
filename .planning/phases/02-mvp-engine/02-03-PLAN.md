---
phase: 02-mvp-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/render/remotion/render-composition.tsx
  - src/render/export/fingerprint.ts
  - tests/render-repro-smoke.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - P02-RENDER-EXPORT-MP4
  - P02-REPRODUCIBLE-ARTIFACT-SHAPE
must_haves:
  truths:
    - User can render/export a deterministic demo sequence to MP4 through Remotion and FFmpeg.
    - User can run reproducibility checks that compare deterministic manifest and output fingerprint for identical inputs.
    - CI fails hard on reproducibility violations and preserves failure evidence bundles.
  artifacts:
    - path: src/render/remotion/render-composition.tsx
      provides: frame-driven render composition consuming deterministic engine state
    - path: src/render/export/fingerprint.ts
      provides: manifest and output fingerprint generation with provenance metadata
    - path: tests/render-repro-smoke.test.ts
      provides: two-run reproducibility gate assertions for canonical scenarios
    - path: .github/workflows/ci.yml
      provides: PR smoke and nightly deterministic matrix gates with artifact retention policies
  key_links:
    - from: src/render/remotion/render-composition.tsx
      to: src/engine/timeline/scheduler.ts
      via: frame state consumption for render determinism
      pattern: "frame|scheduler|stateAtFrame"
    - from: tests/render-repro-smoke.test.ts
      to: src/render/export/fingerprint.ts
      via: two-run fingerprint/manifest equality assertions
      pattern: "manifest|fingerprint|diff bundle"
    - from: .github/workflows/ci.yml
      to: tests/render-repro-smoke.test.ts
      via: mandatory PR/nightly gate execution
      pattern: "smoke|nightly|artifact retention"
---

<objective>
Deliver the deterministic render/export and CI reproducibility gates for the MVP engine.

Purpose: Guarantee that identical spec+seed runs produce consistent artifacts and enforce hard-fail policies when reproducibility drifts.
Output: Render composition, fingerprint tooling, reproducibility smoke tests, and CI gate workflow.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-engine/02-CONTEXT.md
@.planning/phases/02-mvp-engine/02-RESEARCH.md
@.planning/adr/ADR-001-tech-stack.md
@.planning/adr/ADR-003-e2e-testing-strategy.md
@.planning/phases/02-mvp-engine/02-01-SUMMARY.md
@.planning/phases/02-mvp-engine/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic render composition</name>
  <read_first>
    - .planning/adr/ADR-001-tech-stack.md
    - .planning/phases/02-mvp-engine/02-02-SUMMARY.md
  </read_first>
  <files>src/render/remotion/render-composition.tsx</files>
  <behavior>
    - Test 1: composition derives visual state only from deterministic scheduler output and current frame index.
    - Test 2: repeated render invocations with same input generate same timeline trace inputs.
  </behavior>
  <action>Create Remotion composition entry that consumes scheduler/camera/packet deterministic outputs from Plan 02, with no wall-clock state. Use frame index as canonical timebase per D-14 and route all render setup through validated SceneSpec gate from Plan 01. Pattern analog: follow `.planning/phases/02-mvp-engine/02-PATTERNS.md` mapping for `src/render/remotion/render-composition.tsx` (ADR-001 render stack + Remotion CLI shape).</action>
  <verify>
    <automated>npm run render:demo</automated>
  </verify>
  <acceptance_criteria>Render composition executes from deterministic engine state and produces demo frames without nondeterministic side effects.</acceptance_criteria>
  <done>Deterministic demo render path is implemented and callable via scripts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reproducibility manifest and fingerprint module</name>
  <read_first>
    - .planning/phases/02-mvp-engine/02-CONTEXT.md
    - .planning/adr/ADR-003-e2e-testing-strategy.md
  </read_first>
  <files>src/render/export/fingerprint.ts, tests/render-repro-smoke.test.ts</files>
  <behavior>
    - Test 1: identical spec+seed runs emit equal deterministic manifests including provenance fingerprint.
    - Test 2: output fingerprint is computed from frame-sample hashes and normalized container metadata.
    - Test 3: mismatch triggers hard failure and writes diff bundle artifact paths.
  </behavior>
  <action>Implement reproducibility gate per D-16, D-17, D-18, D-29, D-31, D-32: generate deterministic run manifest including runtime/toolchain provenance, compute output fingerprints from frame-sample hash set plus normalized metadata, and fail with diff bundle on mismatches. Add smoke test that runs canonical scenario twice and asserts strict equality policies. Pattern analogs: follow `.planning/phases/02-mvp-engine/02-PATTERNS.md` entries for `src/render/export/fingerprint.ts` and `tests/render-repro-smoke.test.ts` (CONTEXT fingerprint policy + ADR-003 two-run determinism gate).</action>
  <verify>
    <automated>npm run test:e2e:render-smoke</automated>
  </verify>
  <acceptance_criteria>Two-run smoke test passes for identical inputs and deterministically fails with diff evidence when fingerprints diverge.</acceptance_criteria>
  <done>Reproducibility evidence artifacts and hard-fail logic are implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Wire PR/nightly CI reproducibility gates</name>
  <read_first>
    - .planning/adr/ADR-003-e2e-testing-strategy.md
    - .planning/phases/02-mvp-engine/02-RESEARCH.md
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>Define CI workflow with PR smoke matrix (TLS/SSH/DNS reduced frames) and nightly full matrix, enforce hard failure for reproducibility mismatches, pin runtime/toolchain versions per D-30, and retain artifacts 7 days for PR / 14 days for nightly with failure logs+trace hashes+metadata preserved. Pattern analog: follow `.planning/phases/02-mvp-engine/02-PATTERNS.md` entry for `.github/workflows/ci.yml` aligned to ADR-003 pipeline policy.</action>
  <verify>
    <automated>npm test && npm run test:e2e:render-smoke</automated>
  </verify>
  <acceptance_criteria>CI config includes deterministic matrix jobs, pinned versions, and artifact retention/failure bundle behavior matching ADR-003.</acceptance_criteria>
  <done>Automated CI gate protects render/export reproducibility before phase completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine deterministic state -> render pipeline | Incorrect state transfer can introduce hidden nondeterminism |
| render outputs -> fingerprint validator | Artifact tampering or metadata drift can mask reproducibility failures |
| CI runner environment -> reproducibility decision | Toolchain/version drift can invalidate deterministic comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | `src/render/export/fingerprint.ts` | mitigate | Hash frame samples + normalized metadata and compare manifests strictly per D-31 |
| T-02-08 | Repudiation | `tests/render-repro-smoke.test.ts` | mitigate | Require two-run strict equality and diff bundle retention per D-17/D-32 |
| T-02-09 | Denial of Service | `.github/workflows/ci.yml` | mitigate | Pin runtime/toolchain versions and enforce deterministic smoke matrix to catch drift early |
</threat_model>

<verification>
- `npm run render:demo`
- `npm run export:demo`
- `npm run test:e2e:render-smoke`
- `npm test`
</verification>

<success_criteria>
- Render/export flow is deterministic for identical SceneSpec+seed inputs.
- Manifest/fingerprint evidence is produced and enforced as hard gate.
- CI matrix blocks merges on reproducibility regressions and preserves forensic artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mvp-engine/02-03-SUMMARY.md`
</output>
