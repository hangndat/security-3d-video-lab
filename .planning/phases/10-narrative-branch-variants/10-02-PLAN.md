---
phase: 10-narrative-branch-variants
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/content/assemblies/content-depth-branched-v1.json
  - src/fixtures/zero-trust-access-scene-spec.json
  - src/fixtures/oauth-jwt-session-scene-spec.json
  - src/fixtures/api-gateway-waf-scene-spec.json
  - tests/narrative-composition-replay.test.ts
  - tests/long-form-assembly.test.ts
  - scripts/verify-narrative-composition.mjs
autonomous: true
requirements:
  - NARR-01
  - NARR-02
must_haves:
  truths:
    - "content-depth-branched-v1 declares attack-path and defense-path with transition overrides."
    - "Each branch validates and loads with distinct sequences and transition chains."
    - "Deterministic replay tests pass independently per branch with stable trace equality."
    - "verify-narrative-composition.mjs quick mode includes branched assembly coverage."
  artifacts:
    - path: "src/content/assemblies/content-depth-branched-v1.json"
      provides: "Two-branch v1.2 profile (attack-path, defense-path)."
    - path: "tests/narrative-composition-replay.test.ts"
      provides: "Per-branch replay equality and cross-branch divergence assertions."
    - path: "scripts/verify-narrative-composition.mjs"
      provides: "Branched assembly in quick verification gate."
  key_links:
    - from: "content-depth-branched-v1.json"
      to: "validate-long-form-assembly.ts"
      via: "Both branches validate with overrides"
      pattern: "attack-path|defense-path"
    - from: "narrative-composition-replay.test.ts"
      to: "buildLongFormSceneSpec"
      via: "branchId option per NARR-02"
      pattern: "buildDeterministicTraceInputs"
---

<objective>
Ship the branched assembly profile and per-branch deterministic replay verification.

Purpose: Close NARR-01 and NARR-02 with production assembly JSON and CI replay gates.
Output: content-depth-branched-v1 profile, minimal v1.2 scene stubs for stitch replay, extended verify script.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-narrative-branch-variants/10-CONTEXT.md
@.planning/phases/10-narrative-branch-variants/10-01-PLAN.md
@src/content/assemblies/content-depth-long-v1.json
@src/fixtures/golden-scene-spec.json
@tests/narrative-composition-replay.test.ts

<interfaces>
Branch sequences (D-04):

attack-path (7 topics):
`tls → ssh → dns → auth-session → mitm-defense → oauth-jwt-session → api-gateway-waf`

defense-path (8 topics):
`tls → ssh → dns → auth-session → pki-trust-chain → zero-trust-access → oauth-jwt-session → api-gateway-waf`

Transition overrides:
| Branch | fromTopic | toTopic | presetId |
|--------|-----------|---------|----------|
| attack-path | auth-session | mitm-defense | auth-session-to-mitm-defense |
| attack-path | mitm-defense | oauth-jwt-session | mitm-defense-to-oauth-jwt-session |
| defense-path | pki-trust-chain | zero-trust-access | pki-trust-chain-to-zero-trust-access |

Profile metadata:
- slug: content-depth-branched-v1
- defaultBranchId: defense-path
- targetWindowMinutes: { min: 8, max: 12 }
- defaultPacingPresetId: documentary-standard

Stub fixtures: clone golden-scene-spec structure with unique seed + shortened timeline per v1.2 topic for stitch-only replay (full export deferred to Phase 12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author content-depth-branched-v1 assembly profile</name>
  <read_first>
    - .planning/phases/10-narrative-branch-variants/10-CONTEXT.md
    - src/content/assemblies/content-depth-long-v1.json
    - src/content/composition/validate-long-form-assembly.ts
  </read_first>
  <files>
    - src/content/assemblies/content-depth-branched-v1.json
    - tests/long-form-assembly.test.ts
  </files>
  <behavior>
    - Test 1: validateLongFormAssemblyProfile returns zero errors for content-depth-branched-v1.
    - Test 2: loadLongFormAssembly('content-depth-branched-v1', 'attack-path') sequence length is 7.
    - Test 3: loadLongFormAssembly('content-depth-branched-v1', 'defense-path') sequence length is 8.
    - Test 4: attack-path transitions include auth-session→mitm-defense override preset.
  </behavior>
  <action>Create content-depth-branched-v1.json with branches attack-path and defense-path per D-04/D-05. Include transitionOverrides on each branch as needed. Set defaultBranchId to defense-path. Add long-form-assembly.test.ts cases asserting validation, load, and transition coherence for both branches.</action>
  <acceptance_criteria>Branched profile validates; both branches load with correct sequences and override transitions; linear assemblies still pass.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/long-form-assembly.test.ts --testNamePattern="branched|attack-path|defense-path"</automated>
  </verify>
  <done>NARR-01 deliverable: creators can define branch variants in assembly JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Minimal v1.2 scene stubs and per-branch replay tests</name>
  <read_first>
    - tests/narrative-composition-replay.test.ts
    - src/fixtures/mitm-defense-scene-spec.json
    - src/content/composition/build-long-form-scene-spec.ts
  </read_first>
  <files>
    - src/fixtures/zero-trust-access-scene-spec.json
    - src/fixtures/oauth-jwt-session-scene-spec.json
    - src/fixtures/api-gateway-waf-scene-spec.json
    - tests/narrative-composition-replay.test.ts
  </files>
  <behavior>
    - Test 1: attack-path buildLongFormSceneSpec replay produces identical trace inputs on two runs.
    - Test 2: defense-path replay produces identical trace inputs on two runs.
    - Test 3: attack-path and defense-path traces differ (cross-branch divergence).
    - Test 4: stitched sceneId includes branch suffix (content-depth-branched-v1:attack-path).
  </behavior>
  <action>Add minimal deterministic scene-spec JSON fixtures for zero-trust-access, oauth-jwt-session, and api-gateway-waf (unique seeds, valid timeline structure). Extend EXPANSION_SCENES or new BRANCHED_SCENES map covering all topics in both branches. Add replay tests calling buildLongFormSceneSpec('content-depth-branched-v1', scenes, { branchId }) for attack-path and defense-path. Assert buildDeterministicTraceInputs equality across runs and inequality between branches.</action>
  <acceptance_criteria>NARR-02: each branch passes deterministic replay; branches produce distinct stable outputs.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/narrative-composition-replay.test.ts</automated>
  </verify>
  <done>Per-branch replay verification complete.</done>
</task>

<task type="auto">
  <name>Task 3: Extend verify-narrative-composition for branched assembly</name>
  <read_first>
    - scripts/verify-narrative-composition.mjs
    - tests/long-form-assembly.test.ts
  </read_first>
  <files>
    - scripts/verify-narrative-composition.mjs
  </files>
  <behavior>
    - Script quick mode passes with content-depth-branched-v1 in assembly coverage summary.
    - Assembly summary reports branch count or defaultBranchId for branched profiles.
  </behavior>
  <action>Add content-depth-branched-v1 to ASSEMBLY_SLUGS (or derive slugs from readdir). Update summarizeAssemblies to handle branched profiles (branches.length, defaultBranchId). Ensure quick mode gate passes after replay tests include branched coverage. Run `node scripts/verify-narrative-composition.mjs --quick`.</action>
  <acceptance_criteria>verify-narrative-composition.mjs --quick exits 0 with branched assembly listed.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-narrative-composition.mjs --quick</automated>
  </verify>
  <done>CI verification gate covers branched narrative composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stub fixtures → replay tests | Stubs are stitch-only; not production export quality |
| branch replay → artifact hash | Deterministic seeds ensure stable trace inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | D | narrative-composition-replay.test.ts | mitigate | Independent replay per branchId prevents cross-branch bleed. |
| T-10-05 | I | stub scene fixtures | accept | Minimal stubs for stitch replay; Phase 12 adds full export fixtures. |
</threat_model>

<verification>
- [ ] `npm run test -- tests/long-form-assembly.test.ts tests/narrative-composition-replay.test.ts`
- [ ] `node scripts/verify-narrative-composition.mjs --quick`
</verification>

<success_criteria>
- NARR-01: attack-path and defense-path declared in assembly JSON and validate.
- NARR-02: deterministic replay tests pass independently for each branch.
- verify-narrative-composition quick gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-narrative-branch-variants/10-02-SUMMARY.md`
</output>
