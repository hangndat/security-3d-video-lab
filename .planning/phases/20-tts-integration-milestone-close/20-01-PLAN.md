---
phase: 20-tts-integration-milestone-close
plan: 01
type: execute
wave: 1
depends_on:
  - 19-02
files_modified:
  - src/content/narration/providers/elevenlabs-provider.ts
  - src/content/narration/providers/resolve-narration-provider.ts
  - src/content/narration/generate-narration-track.ts
  - src/render/export/generate-tls-production-artifacts.ts
  - src/render/export/build-long-form-export-bundle.ts
  - scripts/verify-tts-integration.mjs
  - tests/tts-provider.test.ts
  - tests/tls-production-export.test.ts
  - tests/narration-track.test.ts
  - package.json
autonomous: true
requirements:
  - PROD-02
must_haves:
  truths:
    - "resolveNarrationProvider returns deterministic-stub when ELEVENLABS_API_KEY unset."
    - "resolveNarrationProvider returns elevenlabs when ELEVENLABS_API_KEY set."
    - "generateTlsProductionArtifacts writes narration-track.json and WAV segments."
    - "validateNarrationAlignment passes for TLS production bundle with stub provider in CI."
    - "ElevenLabs provider tested with mocked fetch; no network in default test run."
  artifacts:
    - path: "src/content/narration/providers/resolve-narration-provider.ts"
      provides: "Env-gated provider selection."
    - path: "src/content/narration/providers/elevenlabs-provider.ts"
      provides: "ElevenLabs TTS NarrationProvider."
    - path: "scripts/verify-tts-integration.mjs"
      provides: "PROD-02 verification gate."
    - path: "tests/tts-provider.test.ts"
      provides: "Resolver and mocked ElevenLabs tests."
  key_links:
    - from: "resolve-narration-provider.ts"
      to: "src/content/narration/providers/deterministic-stub-provider.ts"
      via: "Dummy audio fallback when no API key"
      pattern: "createDeterministicStubProvider"
    - from: "generate-tls-production-artifacts.ts"
      to: "src/content/narration/generate-narration-track.ts"
      via: "Narration track in production bundle"
      pattern: "generateNarrationTrack"
    - from: "elevenlabs-provider.ts"
      to: ".planning/adr/ADR-001-tech-stack.md"
      via: "Hybrid narration strategy"
      pattern: "ElevenLabs"
---

<objective>
Deliver PROD-02: ElevenLabs TTS provider with deterministic dummy-audio fallback when no API key, integrated into TLS production narration artifacts.

Purpose: Enable real TTS locally while keeping CI fully deterministic via silent stub WAV.
Output: Provider resolver, ElevenLabs provider, extended TLS bundle, verify script, PROD-02 tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-tts-integration-milestone-close/20-CONTEXT.md
@.planning/phases/20-tts-integration-milestone-close/20-RESEARCH.md
@src/content/narration/providers/deterministic-stub-provider.ts
@src/content/narration/providers/types.ts
@src/render/export/generate-tls-production-artifacts.ts
@src/verification/tls-production-rubric.ts

<interfaces>
Provider resolver:
```typescript
export type NarrationProviderEnv = {
  ELEVENLABS_API_KEY?: string;
  ELEVENLABS_VOICE_ID?: string;
};

export function resolveNarrationProvider(
  env: NarrationProviderEnv = process.env
): NarrationProvider;
```

ElevenLabs provider:
```typescript
export const ELEVENLABS_PROVIDER_ID = "elevenlabs";

export function createElevenLabsProvider(options: {
  apiKey: string;
  voiceId?: string;
  fetchImpl?: typeof fetch;
}): NarrationProvider;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ElevenLabs provider and resolver</name>
  <read_first>
    - src/content/narration/providers/deterministic-stub-provider.ts
    - .planning/adr/ADR-001-tech-stack.md
  </read_first>
  <files>
    - src/content/narration/providers/elevenlabs-provider.ts
    - src/content/narration/providers/resolve-narration-provider.ts
  </files>
  <action>
    Implement createElevenLabsProvider: POST text-to-speech with scriptIntent, return WAV-compatible bytes (convert MPEG if needed or request pcm/wav format), contentHash from analyticKey+providerId+byte length. Inject fetch for tests.
    Implement resolveNarrationProvider: no ELEVENLABS_API_KEY → createDeterministicStubProvider(); else createElevenLabsProvider. Export ELEVENLABS_PROVIDER_ID and DETERMINISTIC_STUB_PROVIDER_ID re-exports for callers.
  </action>
  <acceptance_criteria>Resolver never calls network without API key; ElevenLabs id registered.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tts-provider.test.ts --testNamePattern="resolveNarrationProvider|elevenlabs"</automated>
  </verify>
  <done>Provider selection and ElevenLabs implementation ready.</done>
</task>

<task type="auto">
  <name>Task 2: Wire resolver into export paths and TLS production bundle</name>
  <read_first>
    - src/render/export/build-long-form-export-bundle.ts
    - src/render/export/generate-tls-production-artifacts.ts
  </read_first>
  <files>
    - src/render/export/generate-tls-production-artifacts.ts
    - src/render/export/build-long-form-export-bundle.ts
    - src/content/narration/generate-narration-track.ts
  </files>
  <action>
    Replace hardcoded createDeterministicStubProvider() in buildLongFormExportBundle with resolveNarrationProvider() (optional env override param for tests).
    Extend generateTlsProductionArtifacts: after caption map, generate narration track + writeNarrationArtifacts to `.artifacts/production/tls/narration/`; update production-manifest.json and audio-layer-handoff.md with providerId; extend security-signoff with narrationProviderId + narrationAlignmentValid.
  </action>
  <acceptance_criteria>TLS production bundle includes narration artifacts; stub used when no key.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts --testNamePattern="narration"</automated>
  </verify>
  <done>Narration integrated into production export path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PROD-02 tests and verify script</name>
  <read_first>
    - tests/narration-track.test.ts
    - scripts/verify-tls-production.mjs
  </read_first>
  <files>
    - tests/tts-provider.test.ts
    - tests/tls-production-export.test.ts
    - tests/narration-track.test.ts
    - scripts/verify-tts-integration.mjs
    - package.json
  </files>
  <behavior>
    - Test 1: resolveNarrationProvider without key returns deterministic-stub id.
    - Test 2: resolveNarrationProvider with key returns elevenlabs id.
    - Test 3: mocked ElevenLabs fetch produces synthesizeSegment result with audioBytes.
    - Test 4: generateTlsProductionArtifacts writes narration-track.json when key unset.
    - Test 5: validateNarrationAlignment passes for TLS production caption map + stub track.
    - Test 6: stub path content hashes unchanged (regression vs Phase 11).
  </behavior>
  <action>
    Create tests/tts-provider.test.ts. Create verify-tts-integration.mjs: run tts tests, assert stub provider in report, write .artifacts/verification/phase20/tts-integration.json. Add npm script verify:tts-integration.
  </action>
  <acceptance_criteria>PROD-02 verifiable in CI without API key.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tts-provider.test.ts tests/tls-production-export.test.ts && node scripts/verify-tts-integration.mjs --quick</automated>
  </verify>
  <done>PROD-02 tests and verify gate passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → provider | API key never logged or committed |
| CI → ElevenLabs | No key in CI; stub only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | I | ELEVENLABS_API_KEY leak | mitigate | Env only; no key in artifacts |
| T-20-02 | D | CI non-determinism | mitigate | Stub default; mocked ElevenLabs tests |
</threat_model>

<verification>
- [ ] `npm run test -- tests/tts-provider.test.ts`
- [ ] `node scripts/verify-tts-integration.mjs --quick`
- [ ] `npm run test -- tests/narration-track.test.ts`
</verification>

<success_criteria>
- PROD-02: ElevenLabs when key present; dummy audio (deterministic-stub) when not; TLS narration aligned.
</success_criteria>

<output>
After completion, create `.planning/phases/20-tts-integration-milestone-close/20-01-SUMMARY.md`
</output>
