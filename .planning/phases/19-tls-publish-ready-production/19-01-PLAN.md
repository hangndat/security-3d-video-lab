---
phase: 19-tls-publish-ready-production
plan: 01
type: execute
wave: 1
depends_on:
  - 18-02
files_modified:
  - src/fixtures/tls-production-scene-spec.json
  - src/render/remotion/render-composition.tsx
  - src/client/viz/compose-scene.tsx
  - src/content/composition/generate-caption-timing-map.ts
  - tests/tls-production-export.test.ts
  - tests/render-composition.test.ts
autonomous: true
requirements:
  - PROD-01
must_haves:
  truths:
    - "tls-production-scene-spec.json validates and covers all five TLS contract beats."
    - "deriveRenderFrameState exposes vizRenderTraceInput from getComposePlan."
    - "renderCompositionProductionMp4 exports full totalFrames at 640×360."
    - "renderCompositionDemoMp4 short profile unchanged (30 frames, golden fixture)."
    - "Viz-aware trace is deterministic across repeated calls per frame."
  artifacts:
    - path: "src/fixtures/tls-production-scene-spec.json"
      provides: "Publish-ready TLS SceneSpec."
    - path: "src/render/remotion/render-composition.tsx"
      provides: "Production MP4 renderer + viz trace."
    - path: "tests/tls-production-export.test.ts"
      provides: "Production SceneSpec and trace tests."
  key_links:
    - from: "render-composition.tsx"
      to: "src/client/viz/compose-scene.tsx"
      via: "getComposePlan for vizRenderTraceInput"
      pattern: "getComposePlan"
    - from: "tls-production-scene-spec.json"
      to: "src/content/topics/tls/contract.json"
      via: "Beat-aligned timeline cues"
      pattern: "tls-server-hello-beat|tls-hook"
    - from: "deriveRenderFrameState"
      to: "src/engine/timeline/scheduler.ts"
      via: "Existing timelineTraceInput preserved"
      pattern: "timelineTraceInput"
---

<objective>
Deliver PROD-01 foundation: TLS production SceneSpec and viz-aware production MP4 render path wired to the Phase 17–18 compose stack.

Purpose: Replace export-gate golden fixture as the publish canonical scene; enable full-duration deterministic export before rubric/artifact gates in 19-02.
Output: Production fixture, extended render composition, production trace tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-tls-publish-ready-production/19-CONTEXT.md
@.planning/phases/19-tls-publish-ready-production/19-RESEARCH.md
@src/content/topics/tls/contract.json
@src/fixtures/golden-scene-spec.json
@src/fixtures/tls-server-hello-scene-spec.json
@src/client/viz/compose-scene.tsx
@src/render/remotion/render-composition.tsx
@docs/tls-crew-walkthrough.md

<interfaces>
Extended render frame state:
```typescript
export interface DeterministicRenderFrameState extends ScheduledFrameState {
  timelineTraceInput: string;
  vizFrameState: VizFrameState;
  vizRenderTraceInput: string; // seed:frame:renderOrder:positionsDigest
}
```

Production render profile:
```typescript
export type ProductionRenderProfile = {
  width: number;   // default 640
  height: number;  // default 360
  fps: number;     // default 30
};

export function renderCompositionProductionMp4(
  sceneSpec: SceneSpec,
  outputPath: string,
  options?: { profile?: ProductionRenderProfile; captionMap?: CaptionTimingMap }
): void;
```

Production SceneSpec beat windows (from contract):
- tls-hook: 0–30
- tls-client-hello-beat: 12–48
- tls-server-hello-beat: 54–98
- tls-finished-beat: 110–168
- tls-app-data-beat: 170–236
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TLS production SceneSpec fixture</name>
  <read_first>
    - src/content/topics/tls/contract.json
    - src/fixtures/tls-server-hello-scene-spec.json
    - .cursor/skills/cinematic-storyboard-artist/templates/shot-list.md
  </read_first>
  <files>
    - src/fixtures/tls-production-scene-spec.json
  </files>
  <action>
    Create tls-production-scene-spec.json: unique seed/sceneId (`tls-production-scene`), actors (client, server), packets with distinct routes per handshake phase, five timeline packet cues with ids containing beat keywords (hook, client-hello, server-hello, finished, app-data) aligned to contract frame windows, packetVariant hints where applicable (threat, encrypted), totalFrames 236.
    Validate mentally against validateSceneSpec schema (actors, packets, timeline, capabilities).
  </action>
  <acceptance_criteria>Fixture covers all five TLS storyboard beat ids via timeline cue naming.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts --testNamePattern="fixture|beat coverage"</automated>
  </verify>
  <done>Publish-ready TLS SceneSpec fixture ready for render.</done>
</task>

<task type="auto">
  <name>Task 2: Viz-aware trace and production MP4 renderer</name>
  <read_first>
    - src/render/remotion/render-composition.tsx
    - src/client/viz/compose-scene.tsx
    - src/content/composition/generate-caption-timing-map.ts
  </read_first>
  <files>
    - src/render/remotion/render-composition.tsx
  </files>
  <action>
    Extend deriveRenderFrameState: call getComposePlan(sceneSpec, frame, { captionMap }) when captionMap provided via optional param or internal TLS caption helper; compute vizRenderTraceInput from renderOrder + sorted packet position/progress digest; preserve timelineTraceInput unchanged for regression.
    Add renderCompositionProductionMp4: loop frame 0..totalFrames-1, use vizRenderTraceInput for colorFromTrace, 640×360 PPM frames, ffmpeg encode. Keep renderCompositionDemoMp4 identical (30 frames, timeline trace only).
    Export helper buildVizRenderTraceInput(sceneSpec, frame, captionMap?) for tests.
  </action>
  <acceptance_criteria>Production render produces MP4 with duration ~totalFrames/30s; short demo unchanged.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts --testNamePattern="vizRenderTrace|production render"</automated>
  </verify>
  <done>Production render path wired to viz compose stack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Production SceneSpec and trace tests</name>
  <read_first>
    - tests/render-composition.test.ts
    - tests/viz-cert-hud-modules.test.ts
  </read_first>
  <files>
    - tests/tls-production-export.test.ts
    - tests/render-composition.test.ts
  </files>
  <behavior>
    - Test 1: tls-production-scene-spec passes validateSceneSpec.
    - Test 2: all five TLS contract beat ids represented in timeline cue ids.
    - Test 3: vizRenderTraceInput deterministic on production fixture frames 0, 75, 200.
    - Test 4: vizRenderTraceInput differs from timelineTraceInput when HUD/cert modules active.
    - Test 5: renderCompositionProductionMp4 writes non-empty MP4 for production fixture.
    - Test 6: deriveRenderFrameState on golden fixture still stable timelineTraceInput (regression).
  </behavior>
  <action>Create tests/tls-production-export.test.ts with describe "PROD-01 production scene".</action>
  <acceptance_criteria>Production foundation verifiable in CI without WebGL.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts tests/render-composition.test.ts</automated>
  </verify>
  <done>19-01 tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production scene → render | validateSceneSpec before encode |
| viz trace → short render | Demo path must not call production profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | D | short render regression | mitigate | Golden fixture regression test |
| T-19-02 | T | invalid production scene | mitigate | validateSceneSpec + beat coverage test |
</threat_model>

<verification>
- [ ] `npm run test -- tests/tls-production-export.test.ts --testNamePattern="PROD-01"`
- [ ] `npm run test -- tests/render-composition.test.ts`
- [ ] `npm run test -- tests/e2e-canonical-flows.test.ts --testNamePattern="TLS canonical"`
</verification>

<success_criteria>
- TLS production SceneSpec and viz-aware full-duration render path operational.
</success_criteria>

<output>
After completion, create `.planning/phases/19-tls-publish-ready-production/19-01-SUMMARY.md`
</output>
