---
phase: 19-tls-publish-ready-production
plan: 02
type: execute
wave: 1
depends_on:
  - 19-01
files_modified:
  - src/verification/export-quality.ts
  - src/verification/tls-production-rubric.ts
  - src/render/export/generate-tls-production-artifacts.ts
  - scripts/verify-tls-production.mjs
  - docs/tls-crew-walkthrough.md
  - tests/tls-production-export.test.ts
  - package.json
autonomous: true
requirements:
  - PROD-01
must_haves:
  truths:
    - "PRODUCTION_EXPORT_QUALITY_POLICY asserts duration beyond short CI window."
    - "Security rubric validates beat→module mapping for all five TLS beats."
    - "Crew pipeline artifacts generated under .artifacts/production/tls/."
    - "verify-tls-production.mjs passes and writes phase19 evidence JSON."
    - "validateNarrationAlignment passes on TLS production caption + stub narration."
    - "PROD-01 marked Complete in REQUIREMENTS.md."
  artifacts:
    - path: "src/verification/tls-production-rubric.ts"
      provides: "Production quality + security sign-off assertions."
    - path: "src/render/export/generate-tls-production-artifacts.ts"
      provides: "Crew artifact bundle generator."
    - path: "scripts/verify-tls-production.mjs"
      provides: "Phase 19 verification gate."
    - path: "tests/tls-production-export.test.ts"
      provides: "Full PROD-01 integration tests."
  key_links:
    - from: "tls-production-rubric.ts"
      to: "docs/security-accuracy-checklist.md"
      via: "Per-beat module expectations"
      pattern: "viz-cert-single|viz-packet-threat"
    - from: "generate-tls-production-artifacts.ts"
      to: "docs/tls-crew-walkthrough.md"
      via: "Artifact paths referenced in walkthrough"
      pattern: "artifacts/production/tls"
    - from: "verify-tls-production.mjs"
      to: "src/verification/export-quality.ts"
      via: "assertExportQuality with production policy"
      pattern: "PRODUCTION_EXPORT_QUALITY_POLICY"
---

<objective>
Deliver PROD-01 completion: production quality rubric, automated TLS security sign-off, crew pipeline artifact bundle, and verification gate.

Purpose: Prove publish-ready TLS module exceeds export-gate thresholds with documented crew chain outputs.
Output: Rubric module, artifact generator, verify script, walkthrough update, PROD-01 tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-tls-publish-ready-production/19-CONTEXT.md
@.planning/phases/19-tls-publish-ready-production/19-RESEARCH.md
@docs/security-accuracy-checklist.md
@docs/tls-crew-walkthrough.md
@src/verification/export-quality.ts
@src/content/narration/validate-narration-alignment.ts
@src/content/narration/generate-narration-track.ts
@scripts/verify-crew-skills.mjs

<interfaces>
Production quality policy:
```typescript
export const PRODUCTION_EXPORT_QUALITY_POLICY: ExportQualityPolicy = {
  codec: "h264",
  container: "mp4",
  minDurationSeconds: 7.0,
  maxDurationSeconds: 9.0
};

export function productionPolicyForScene(sceneSpec: SceneSpec): ExportQualityPolicy;
```

Security rubric:
```typescript
export type TlsBeatModuleExpectation = {
  beatId: string;
  representativeFrame: number;
  expectedModules: string[]; // subset of renderOrder
};

export function assertTlsProductionRubric(
  sceneSpec: SceneSpec,
  captionMap: CaptionTimingMap
): { signoff: TlsSecuritySignoff };
```

Artifact manifest:
```typescript
export function generateTlsProductionArtifacts(repoRoot: string): {
  artifactRoot: string;
  mp4Path: string;
  manifestPath: string;
  securitySignoffPath: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production quality policy and security rubric</name>
  <read_first>
    - src/verification/export-quality.ts
    - docs/security-accuracy-checklist.md
    - src/client/viz/compose-scene.tsx
  </read_first>
  <files>
    - src/verification/export-quality.ts
    - src/verification/tls-production-rubric.ts
  </files>
  <action>
    Add PRODUCTION_EXPORT_QUALITY_POLICY and productionPolicyForScene(sceneSpec) computing min/max from totalFrames/fps ± tolerance.
    Implement tls-production-rubric.ts: assertTlsBeatCoverage (five beats in timeline), assertTlsModuleMapping at representative frames (hook→threat, client-hello→flow+handshake, server-hello→cert+handshake, finished→secure, app-data→encrypted+secure), assertNarrationAlignment via generateCaptionTimingMap + generateNarrationTrack stub + validateNarrationAlignment.
    Export TlsSecuritySignoff JSON shape for security-signoff.json.
  </action>
  <acceptance_criteria>Rubric throws on missing beat or wrong module; passes on production fixture.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts --testNamePattern="rubric|security"</automated>
  </verify>
  <done>Production quality and security gates implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Crew artifact generator and verify script</name>
  <read_first>
    - scripts/verify-crew-skills.mjs
    - .cursor/skills/cinematic-production-orchestrator/templates/pipeline-checklist.md
    - src/render/export/build-long-form-export-bundle.ts
  </read_first>
  <files>
    - src/render/export/generate-tls-production-artifacts.ts
    - scripts/verify-tls-production.mjs
    - package.json
  </files>
  <action>
    Implement generateTlsProductionArtifacts: copy/reference production scene spec, render MP4 via renderCompositionProductionMp4, write beat-sheet.md / render-handoff.md / audio-layer-handoff.md snapshots with repo paths and TLS beat ids, write security-signoff.json and production-manifest.json.
    Create verify-tls-production.mjs: run artifact generation, assertExportQuality with production policy, run rubric, write .artifacts/verification/phase19/tls-production.json. Add npm script verify:tls-production.
  </action>
  <acceptance_criteria>verify script exit 0; artifact root contains MP4 + manifest + signoff.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-tls-production.mjs --quick</automated>
  </verify>
  <done>Crew pipeline artifacts and verify gate operational.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PROD-01 integration tests and walkthrough update</name>
  <read_first>
    - tests/tls-production-export.test.ts
    - docs/tls-crew-walkthrough.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    - tests/tls-production-export.test.ts
    - docs/tls-crew-walkthrough.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <behavior>
    - Test 1: renderCompositionProductionMp4 + assertExportQuality passes PRODUCTION policy.
    - Test 2: assertTlsProductionRubric passes on production fixture + TLS caption map.
    - Test 3: generateTlsProductionArtifacts writes expected file set.
    - Test 4: security-signoff.json lists five beats all passed.
    - Test 5: full npm test suite green (regression).
  </behavior>
  <action>
    Extend tests with describe "PROD-01 publish-ready TLS". Update tls-crew-walkthrough Step 3/5 to cite tls-production-scene-spec.json as publish canonical; Step 5 to reference production profile and verify:tls-production. Mark PROD-01 Complete in REQUIREMENTS.md.
  </action>
  <acceptance_criteria>PROD-01 structurally and automatically verifiable.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/tls-production-export.test.ts && npm run validate:requirements</automated>
  </verify>
  <done>Phase 19 complete; PROD-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rubric → contract | Read-only beat objectives |
| artifacts → repo | Write only under .artifacts/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | I | false security sign-off | mitigate | Module mapping tests at rep frames |
| T-19-04 | D | duration policy flake | mitigate | Tolerance band from totalFrames |
</threat_model>

<verification>
- [ ] `npm run test -- tests/tls-production-export.test.ts`
- [ ] `node scripts/verify-tls-production.mjs --quick`
- [ ] `npm run validate:requirements`
- [ ] `npm test`
</verification>

<success_criteria>
- PROD-01: TLS module publish-ready with crew artifacts, security sign-off, and production quality assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/19-tls-publish-ready-production/19-02-SUMMARY.md`
</output>
