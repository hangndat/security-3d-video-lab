---
phase: 05-content-authoring-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/scaffold-topic-contract.mjs
  - src/content/topics/auth-session/contract.json
  - src/content/topics/pki-trust-chain/contract.json
  - src/content/topics/mitm-defense/contract.json
  - src/content/topics/manifest.json
  - tests/content-authoring-foundation.test.ts
  - tests/e2e-canonical-flows.test.ts
  - scripts/verify-content-authoring.mjs
  - .github/workflows/ci.yml
  - .planning/phases/05-content-authoring-foundation/VERIFICATION.md
  - .artifacts/verification/phase05/content-authoring-foundation.json
autonomous: true
requirements:
  - CONT-01
  - AUTHR-01
  - AUTHR-02
must_haves:
  truths:
    - "Creator can scaffold a new topic contract using CLI and topic-centric folder layout."
    - "At least three new non-TLS/SSH/DNS module drafts validate in CI without renderer code edits."
    - "Phase gate blocks merges unless contract tests and E2E smoke pass, with dual-format evidence."
  artifacts:
    - path: "scripts/scaffold-topic-contract.mjs"
      provides: "CLI scaffolder for one-file topic contract generation with beat stubs."
    - path: "src/content/topics/manifest.json"
      provides: "Manifest-locked long-form order consumed by validator."
    - path: "src/content/topics/auth-session/contract.json"
      provides: "New topic draft contract using naming/version and preset rules."
    - path: "src/content/topics/pki-trust-chain/contract.json"
      provides: "New topic draft contract using naming/version and preset rules."
    - path: "src/content/topics/mitm-defense/contract.json"
      provides: "New topic draft contract using naming/version and preset rules."
    - path: "scripts/verify-content-authoring.mjs"
      provides: "JSON + markdown verification artifact generator for phase evidence."
  key_links:
    - from: "scripts/scaffold-topic-contract.mjs"
      to: "src/content/contracts/topic-contract.schema.json"
      via: "Generated template matches schema and validator expectations"
      pattern: "schemaVersion|slug|storyboardBeats"
    - from: "tests/content-authoring-foundation.test.ts"
      to: "src/content/topics/manifest.json"
      via: "Manifest lock asserted against loaded topic order"
      pattern: "manifest|order|validateTopicContracts"
    - from: ".github/workflows/ci.yml"
      to: "scripts/verify-content-authoring.mjs"
      via: "PR full validation invokes verification and evidence generation"
      pattern: "pull_request|npm run test|verify-content-authoring"
---

<objective>
Operationalize the authoring workflow so new security modules are generated via scaffold CLI, validated in blocking CI, and recorded with both machine and human verification evidence.

Purpose: Deliver D-10 through D-13 and satisfy CONT-01 by proving expansion-ready authoring contracts for at least three new module drafts.
Output: CLI scaffold flow, three new topic contract drafts, manifest lock wiring, blocking test/CI updates, and dual-format verification evidence.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-content-authoring-foundation/05-CONTEXT.md
@.planning/phases/05-content-authoring-foundation/05-01-PLAN.md
@tests/e2e-canonical-flows.test.ts

<interfaces>
From 05-01 validator outputs:
- Contract validation surface must expose deterministic `errors[]` and `warnings[]` collections for machine and markdown evidence generation.
- Manifest ordering checks must be callable from test and CLI contexts.

From tests/e2e-canonical-flows.test.ts:
- Existing smoke checks (`TLS`, `SSH`, `DNS`) remain mandatory and must continue passing as part of D-11 blocking gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scaffold CLI and manifest-locked topic draft flow</name>
  <read_first>
    - .planning/phases/05-content-authoring-foundation/05-CONTEXT.md
    - .planning/phases/05-content-authoring-foundation/05-01-PLAN.md
  </read_first>
  <files>
    - scripts/scaffold-topic-contract.mjs
    - src/content/topics/manifest.json
    - src/content/topics/auth-session/contract.json
    - src/content/topics/pki-trust-chain/contract.json
    - src/content/topics/mitm-defense/contract.json
  </files>
  <behavior>
    - Test 1: scaffold command creates topic folder + contract template + beat stubs (D-10).
    - Test 2: generated slug follows `<topic>-short-v<major>` naming (D-09).
    - Test 3: manifest lock rejects reordering not present in manifest file (D-07).
  </behavior>
  <action>Implement scaffolder CLI with deterministic template output for one-file topic contracts under `src/content/topics/<topic>/contract.json` (D-02, D-08, D-10). Create three new module drafts beyond TLS/SSH/DNS (`auth-session`, `pki-trust-chain`, `mitm-defense`) to satisfy CONT-01, each using approved transition presets (D-03), strict schemaVersion literal (D-04), and valid beat placeholder structure (CONT-03 linkage).</action>
  <acceptance_criteria>CLI can create a new topic contract end-to-end with required fields, and the three drafted modules validate against schema and manifest lock without renderer modifications.</acceptance_criteria>
  <verify>
    <automated>node scripts/scaffold-topic-contract.mjs --topic auth-session --major 1 --dry-run && npm run test -- tests/content-authoring-foundation.test.ts --testNamePattern="scaffold|manifest|draft"</automated>
  </verify>
  <done>Authoring workflow is executable from CLI and produces valid topic contracts in the required folder and naming scheme.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce blocking validation gate and PR full validation policy</name>
  <read_first>
    - tests/e2e-canonical-flows.test.ts
    - package.json
  </read_first>
  <files>
    - tests/content-authoring-foundation.test.ts
    - tests/e2e-canonical-flows.test.ts
    - .github/workflows/ci.yml
  </files>
  <behavior>
    - Test 1: contract suite fails PR when any blocking validation error exists (D-11).
    - Test 2: CI PR workflow runs full validation every time (D-13).
    - Test 3: E2E smoke remains part of required pass criteria (D-11).
  </behavior>
  <action>Create authoring-foundation integration tests that load all topic contracts (including new drafts), run collect-all validation, and fail on blocking errors while allowing warning-only cases. Update CI workflow so every pull request runs full contract validation plus existing E2E canonical smoke coverage (D-11, D-13), with no downgrade to optional checks.</action>
  <acceptance_criteria>PR pipeline contains mandatory full validation steps and fails deterministically when contract or E2E checks fail.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/content-authoring-foundation.test.ts tests/e2e-canonical-flows.test.ts && rg "pull_request" .github/workflows/ci.yml</automated>
  </verify>
  <done>Blocking phase gate is technically enforced and cannot be bypassed by warning-only or partial validation runs.</done>
</task>

<task type="auto">
  <name>Task 3: Generate dual-format verification evidence artifacts</name>
  <read_first>
    - .planning/phases/05-content-authoring-foundation/05-CONTEXT.md
    - tests/content-authoring-foundation.test.ts
  </read_first>
  <files>
    - scripts/verify-content-authoring.mjs
    - .artifacts/verification/phase05/content-authoring-foundation.json
    - .planning/phases/05-content-authoring-foundation/VERIFICATION.md
  </files>
  <action>Implement a verification script that executes contract+E2E checks and writes (1) machine-readable JSON results and (2) human-readable `VERIFICATION.md` summary with pass/fail tables, warning counts, and module coverage details (D-12). Ensure evidence includes the three new module draft IDs and explicit gate status tied to D-11 blocking criteria.</action>
  <acceptance_criteria>Both JSON and markdown evidence files are generated from the same command and contain consistent outcomes and module-level coverage.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-content-authoring.mjs && npm run test -- tests/content-authoring-foundation.test.ts</automated>
  </verify>
  <done>Phase verification evidence is reproducible, dual-format, and directly consumable for milestone governance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input -> scaffold generator | Untrusted topic names/flags drive file generation paths |
| CI runner -> verification artifacts | Automated test output is converted into governance evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | T | `scripts/scaffold-topic-contract.mjs` | mitigate | Sanitize topic slug inputs and enforce topic-folder confinement to prevent path traversal writes. |
| T-05-06 | R | `scripts/verify-content-authoring.mjs` | mitigate | Record deterministic test command outputs and module IDs in JSON + markdown for auditability. |
| T-05-07 | D | `.github/workflows/ci.yml` | mitigate | Require full validation job on PR and fail-fast only after all required suites finish reporting status. |
| T-05-08 | S | `src/content/topics/*/contract.json` | mitigate | Enforce schemaVersion literal + schema validation before contracts are trusted in batch assembly. |
</threat_model>

<verification>
- `npm run test -- tests/content-authoring-foundation.test.ts`
- `npm run test -- tests/e2e-canonical-flows.test.ts`
- `node scripts/verify-content-authoring.mjs`
</verification>

<success_criteria>
- CONT-01 achieved: three new security module drafts beyond TLS/SSH/DNS validate from data-first contracts.
- AUTHR-01 reinforced: new modules are authored/scaffolded in topic JSON contracts without core renderer edits.
- AUTHR-02 reinforced: gating tests and CI fail with clear validation diagnostics and ordering/duration rule enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-authoring-foundation/05-02-SUMMARY.md`
</output>
