---
phase: 05-content-authoring-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/contracts/topic-contract.schema.json
  - src/content/contracts/transition-presets.ts
  - src/content/contracts/load-topic-contracts.ts
  - src/content/contracts/validate-topic-contracts.ts
  - src/content/contracts/types.ts
  - src/content/batch/first-content-batch.ts
  - tests/content-contracts.test.ts
autonomous: true
requirements:
  - AUTHR-01
  - AUTHR-02
  - CONT-03
must_haves:
  truths:
    - "Creator can define topic modules as JSON contracts without changing core render logic."
    - "Validation reports all contract violations in one pass with clear paths and reasons."
    - "Narration placeholder coverage, duration windows, and topic order are enforced by contract validation."
  artifacts:
    - path: "src/content/contracts/topic-contract.schema.json"
      provides: "Strict JSON schema with schemaVersion literal, one-file topic contract shape, naming, and beat constraints."
    - path: "src/content/contracts/validate-topic-contracts.ts"
      provides: "Collect-all validator with warn/fail duration policy and manifest-order checks."
    - path: "src/content/contracts/load-topic-contracts.ts"
      provides: "Topic folder discovery and JSON parsing from src/content/topics/<topic>/contract.json."
    - path: "tests/content-contracts.test.ts"
      provides: "Automated validation regression coverage for hard failures and warning-only drift."
  key_links:
    - from: "src/content/contracts/load-topic-contracts.ts"
      to: "src/content/contracts/validate-topic-contracts.ts"
      via: "Loaded contracts array passed into validator"
      pattern: "validateTopicContracts\\("
    - from: "src/content/contracts/validate-topic-contracts.ts"
      to: "src/content/batch/first-content-batch.ts"
      via: "Validated contracts converted into batch packet structures"
      pattern: "fromTopicContract|toTopicPacket|build.*Batch"
    - from: "tests/content-contracts.test.ts"
      to: "src/content/contracts/validate-topic-contracts.ts"
      via: "Vitest assertions over aggregated issues and severity"
      pattern: "validateTopicContracts"
---

<objective>
Establish the Phase 05 contract engine so content is authored as JSON files and validated deterministically before render/export paths are executed.

Purpose: Deliver the data-first authoring contract foundation and strict validation behavior defined in D-01 through D-09.
Output: JSON schema, preset catalog, contract loader/validator modules, and automated tests proving clear fail/warn behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-content-authoring-foundation/05-CONTEXT.md
@src/content/batch/first-content-batch.ts
@tests/first-content-batch.test.ts

<interfaces>
From src/content/batch/first-content-batch.ts:
- `TopicPacket` requires `topic`, `slug`, `hook`, `cta`, `durationBudget`, and `storyboardBeats`.
- `NarrationPlaceholder` binds each `beatId` to narration metadata and timing.
- `validateBatchCompleteness()` currently returns `string[]` errors and is the acceptance aggregation precedent.

From package.json:
- `ajv` is available and should be used as the schema validator engine for JSON contracts (per D-01 + existing dependency).
- `vitest` is the required test runner and must drive deterministic contract checks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create JSON contract schema and transition preset catalog</name>
  <read_first>
    - .planning/phases/05-content-authoring-foundation/05-CONTEXT.md
    - src/content/batch/first-content-batch.ts
  </read_first>
  <files>
    - src/content/contracts/topic-contract.schema.json
    - src/content/contracts/transition-presets.ts
    - src/content/contracts/types.ts
  </files>
  <behavior>
    - Test 1: schema rejects non-literal schemaVersion and invalid slug format.
    - Test 2: schema rejects beat arrays with missing IDs or invalid frame windows.
    - Test 3: preset validator rejects transition IDs not in required catalog.
  </behavior>
  <action>Implement D-01, D-02, D-03, D-04, D-08, and D-09 by defining one-file-per-topic JSON contract schema with strict `schemaVersion` literal, topic-centric path assumptions, slug format `<topic>-short-v<major>`, and required transition preset IDs. Implement compatibility metadata in `transition-presets.ts` so each preset declares allowed `fromTopic`/`toTopic` pairs and can be validated without runtime heuristics.</action>
  <acceptance_criteria>Schema and preset modules compile, enforce strict literal versioning, and expose exported contract types/guards used by downstream loader/validator code.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/content-contracts.test.ts --testNamePattern="schema|preset"</automated>
  </verify>
  <done>Contract schema and transition catalog are committed and can reject malformed topic files before any render pipeline execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement topic contract loader and collect-all validator</name>
  <read_first>
    - src/content/contracts/topic-contract.schema.json
    - src/content/batch/first-content-batch.ts
    - tests/first-content-batch.test.ts
  </read_first>
  <files>
    - src/content/contracts/load-topic-contracts.ts
    - src/content/contracts/validate-topic-contracts.ts
    - src/content/batch/first-content-batch.ts
  </files>
  <behavior>
    - Test 1: validator returns all schema/order/placeholder failures in one report (D-05).
    - Test 2: mild duration drift produces warnings only; hard drift produces blocking failures (D-06).
    - Test 3: sequence mismatch against manifest lock fails and never auto-reorders (D-07).
  </behavior>
  <action>Implement filesystem loader targeting `src/content/topics/<topic>/contract.json` (D-08) and a validation pipeline that aggregates all issues before returning. Apply soft-window policy with explicit thresholds (warn at >10% from target, fail beyond hard min/max) per D-06, enforce manifest-locked ordering per D-07, and ensure narration placeholder coverage checks satisfy CONT-03. Refactor `first-content-batch.ts` to consume validated contracts via mapping helpers rather than hardcoded packet arrays (D-01, AUTHR-01).</action>
  <acceptance_criteria>Loader discovers contracts by topic folder, validator returns structured `{ errors, warnings }` with full paths, and batch construction uses validated contracts without direct manual packet definitions.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/content-contracts.test.ts --testNamePattern="collect-all|duration|ordering|placeholder"</automated>
  </verify>
  <done>Data-first contracts become the source of truth for packet assembly, and validation behavior matches D-05, D-06, D-07 exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic contract validation tests and migration coverage</name>
  <read_first>
    - src/content/contracts/load-topic-contracts.ts
    - src/content/contracts/validate-topic-contracts.ts
    - tests/first-content-batch.test.ts
  </read_first>
  <files>
    - tests/content-contracts.test.ts
    - tests/first-content-batch.test.ts
  </files>
  <behavior>
    - Test 1: valid contracts yield zero blocking errors.
    - Test 2: invalid contracts include all expected error messages in a single run.
    - Test 3: existing batch completeness checks still pass when fed from JSON contract loader.
  </behavior>
  <action>Create dedicated tests for schema correctness, transition preset enforcement, collect-all validation output, and soft-window warn/fail behavior. Update existing content-batch tests to assert the data source is loader-driven while preserving deterministic acceptance gates from phase 03 baseline.</action>
  <acceptance_criteria>Contract-focused tests fail on regressions and pass on valid fixtures; legacy batch tests remain green with JSON-driven input path.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/content-contracts.test.ts tests/first-content-batch.test.ts</automated>
  </verify>
  <done>Automated regression suite proves the foundation is deterministic, comprehensive, and executable by CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author JSON contract -> validator | Untrusted content definitions cross into executable pipeline decisions |
| contract loader -> batch assembly | Parsed file content crosses into render/export data structures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `src/content/contracts/load-topic-contracts.ts` | mitigate | Reject unknown schema versions and malformed JSON before mapping into runtime models. |
| T-05-02 | D | `src/content/contracts/validate-topic-contracts.ts` | mitigate | Aggregate and bound validation passes; return deterministic issue arrays instead of retry loops. |
| T-05-03 | R | `tests/content-contracts.test.ts` | mitigate | Assert deterministic error/warning outputs so contract decisions are auditable and reproducible. |
| T-05-04 | E | `src/content/contracts/transition-presets.ts` | mitigate | Enforce whitelist-only transition presets and compatibility checks to prevent unauthorized transition behavior. |
</threat_model>

<verification>
- `npm run test -- tests/content-contracts.test.ts tests/first-content-batch.test.ts`
- `npm run test:quick`
</verification>

<success_criteria>
- AUTHR-01 achieved: topic contracts are defined in JSON files under topic folders and consumed without core render edits.
- AUTHR-02 achieved: validation fails fast at phase gate with detailed multi-error reporting while preserving warning semantics for soft drift.
- CONT-03 achieved: narration placeholder coverage is enforced for every beat across loaded contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-authoring-foundation/05-01-SUMMARY.md`
</output>
