---
phase: 22-scene-builder-parity
plan: 02
type: execute
wave: 1
depends_on:
  - 22-01
files_modified:
  - src/client/viz/viz-mesh-spec.ts
  - src/client/viz/style-tokens.ts
  - src/client/viz/cert/viz-cert-single.tsx
  - src/client/viz/cert/viz-cert-chain.tsx
  - src/client/viz/hud/viz-hud-actor-label.tsx
  - src/client/viz/hud/viz-hud-beat-caption.tsx
  - src/client/viz/hud/viz-hud-packet-id.tsx
  - src/client/viz/hud/viz-hud-frame-counter.tsx
  - src/render/headless/build-viz-three-scene.ts
  - tests/viz-mesh-spec.test.ts
  - tests/headless-scene-parity.test.ts
  - tests/viz-cert-hud-modules.test.ts
  - scripts/verify-headless-scene-parity.mjs
  - package.json
  - docs/style-bible.md
autonomous: true
requirements:
  - RENDER-02
must_haves:
  truths:
    - "All eleven CATALOG_VIZ_MODULE_IDS produce meshes when active in compose plan."
    - "STYLE_TOKENS includes lightKeyIntensity, lightAmbientColor, lightAmbientIntensity, cameraPositionDefault."
    - "build-viz-three-scene uses style tokens for lighting and camera (no hardcoded light intensities)."
    - "Cert/HUD headless meshes mirror R3F placeholder geometry and userData props."
    - "verify-headless-scene-parity.mjs writes phase22/scene-parity.json with gateStatus pass."
  artifacts:
    - path: "scripts/verify-headless-scene-parity.mjs"
      provides: "RENDER-02 verification gate."
    - path: ".artifacts/verification/phase22/scene-parity.json"
      provides: "Machine-readable Phase 22 evidence."
    - path: "tests/headless-scene-parity.test.ts"
      provides: "Full catalog headless parity tests."
  key_links:
    - from: "build-viz-three-scene.ts"
      to: "compose-scene.tsx"
      via: "actors, activeCaption, frame props for cert/HUD"
      pattern: "getComposePlan"
    - from: "viz-mesh-spec.ts"
      to: "src/client/viz/cert/*.tsx"
      via: "CERT_MESH_SPEC geometry import"
      pattern: "viz-mesh-spec"
    - from: "verify-headless-scene-parity.mjs"
      to: "tests/headless-scene-parity.test.ts"
      via: "Gate orchestrator"
      pattern: "headless-scene-parity"
---

<objective>
Complete RENDER-02: cert/HUD mesh factory, lighting/camera token parity, full eleven-module headless coverage, and verification gate.

Purpose: Prove headless scene builder matches R3F compose plan for entire catalog before TLS 3D export (Phase 23).
Output: Extended factory, tokenized lighting/camera, parity tests, verify script.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-scene-builder-parity/22-CONTEXT.md
@.planning/phases/22-scene-builder-parity/22-RESEARCH.md
@src/client/viz/viz-mesh-spec.ts
@src/render/headless/build-viz-three-scene.ts
@src/client/viz/cert/viz-cert-single.tsx
@src/client/viz/hud/viz-hud-beat-caption.tsx
@tests/viz-cert-hud-modules.test.ts
@scripts/verify-headless-capture.mjs
@src/fixtures/tls-production-scene-spec.json

<interfaces>
Extended build context:
```typescript
export type VizMeshBuildContext = {
  sceneSpec: SceneSpec;
  frame: number;
  plan: ComposePlan;
};
```

Verify JSON shape:
```typescript
{
  generatedAt: string;
  gateStatus: "pass" | "fail";
  catalogCoverage: { expected: 11; covered: number };
  blockingCriteria: { meshSpecTestsPassed: boolean; headlessParityTestsPassed: boolean; certHudTestsPassed: boolean };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend viz-mesh-spec for cert and HUD modules</name>
  <read_first>
    - src/client/viz/cert/*.tsx
    - src/client/viz/hud/*.tsx
    - src/client/viz/viz-mesh-spec.ts
  </read_first>
  <files>
    - src/client/viz/viz-mesh-spec.ts
    - src/client/viz/cert/viz-cert-single.tsx
    - src/client/viz/cert/viz-cert-chain.tsx
    - src/client/viz/hud/viz-hud-actor-label.tsx
    - src/client/viz/hud/viz-hud-beat-caption.tsx
    - src/client/viz/hud/viz-hud-packet-id.tsx
    - src/client/viz/hud/viz-hud-frame-counter.tsx
    - tests/viz-mesh-spec.test.ts
  </files>
  <action>
    Add CERT_MESH_SPEC and HUD_MESH_SPEC with geometry/position constants from R3F components. Export headless builders that create placeholder planes/boxes with userData matching R3F (certLabel, caption, beatId, label, frame). Refactor cert/HUD R3F components to import geometry from spec. Extend viz-mesh-spec.test.ts: `CATALOG_VIZ_MODULE_IDS.every(id => id in VIZ_MESH_SPEC)`.
  </action>
  <acceptance_criteria>All 11 catalog ids in VIZ_MESH_SPEC; viz-cert-hud-modules tests pass.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-mesh-spec.test.ts tests/viz-cert-hud-modules.test.ts</automated>
  </verify>
  <done>Full catalog geometry SOT.</done>
</task>

<task type="auto">
  <name>Task 2: Lighting, camera tokens, and full headless module dispatch</name>
  <read_first>
    - docs/style-bible.md
    - src/client/viz/style-tokens.ts
    - src/render/headless/build-viz-three-scene.ts
  </read_first>
  <files>
    - src/client/viz/style-tokens.ts
    - src/render/headless/build-viz-three-scene.ts
    - docs/style-bible.md
    - tests/headless-scene-parity.test.ts
  </files>
  <action>
    Add lighting/camera tokens to STYLE_TOKENS and document in style-bible. Refactor buildVizThreeScene: use tokens for ambient/key light and camera position; pass VizMeshBuildContext (sceneSpec, frame, plan) to factory for all modules in renderOrder. Add GL-gated tests on tls-production-scene-spec frames 75 and 200: scene mesh count increases when cert/HUD modules active vs packet-only frame; iterate renderOrder and assert factory handles each id without throwing.
  </action>
  <acceptance_criteria>No hardcoded `#` or light intensity literals in build-viz-three-scene; all renderOrder ids dispatch to factory.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/headless-scene-parity.test.ts tests/headless-capture.test.ts</automated>
  </verify>
  <done>Eleven-module headless builder with tokenized lighting/camera.</done>
</task>

<task type="auto">
  <name>Task 3: verify-headless-scene-parity gate and npm script</name>
  <read_first>
    - scripts/verify-headless-capture.mjs
    - scripts/verify-v14-viz-modules.mjs
  </read_first>
  <files>
    - scripts/verify-headless-scene-parity.mjs
    - package.json
    - tests/headless-scene-parity.test.ts
  </files>
  <action>
    Create verify-headless-scene-parity.mjs: run viz-mesh-spec + headless-scene-parity + viz-cert-hud-modules tests; write `.artifacts/verification/phase22/scene-parity.json` with catalogCoverage 11/11. Add `npm run verify:headless-scene-parity`. Gate passes when spec keys cover all catalog ids and non-GL tests pass (GL tests skip gracefully).
  </action>
  <acceptance_criteria>node scripts/verify-headless-scene-parity.mjs --quick exits 0.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-mesh-spec.test.ts && node scripts/verify-headless-scene-parity.mjs --quick</automated>
  </verify>
  <done>RENDER-02 verification gate operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HUD text | Placeholder planes only — no font rendering in headless |
| CI | Gate must not require GL for pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | D | CI GL flake | mitigate | skipIf + spec tests without GL |
| T-22-04 | T | Incomplete catalog | mitigate | 11/11 key assertion in verify script |
</threat_model>

<verification>
- [ ] `node scripts/verify-headless-scene-parity.mjs --quick`
- [ ] `npm run test -- tests/viz-mesh-spec.test.ts`
- [ ] `npm run test -- tests/headless-scene-parity.test.ts`
- [ ] `npm run test -- tests/viz-cert-hud-modules.test.ts`
</verification>

<success_criteria>
- RENDER-02: All eleven viz modules render in headless GL from shared mesh SOT; machine-verifiable via phase22 JSON gate.
</success_criteria>

<output>
After completion, create `.planning/phases/22-scene-builder-parity/22-02-SUMMARY.md`
</output>
