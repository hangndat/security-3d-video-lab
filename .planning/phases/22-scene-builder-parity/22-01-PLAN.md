---
phase: 22-scene-builder-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/viz/viz-mesh-spec.ts
  - src/client/viz/packet/viz-packet-flow.tsx
  - src/client/viz/packet/viz-packet-encrypted.tsx
  - src/client/viz/packet/viz-packet-threat.tsx
  - src/client/viz/tunnel/viz-tunnel-secure.tsx
  - src/client/viz/tunnel/viz-tunnel-handshake.tsx
  - src/render/headless/build-viz-three-scene.ts
  - tests/viz-mesh-spec.test.ts
  - tests/headless-scene-parity.test.ts
autonomous: true
requirements:
  - RENDER-02
must_haves:
  truths:
    - "VIZ_MESH_SPEC defines geometry for all five packet/tunnel module ids."
    - "R3F packet and tunnel components import geometry args from viz-mesh-spec."
    - "build-viz-three-scene uses factory builders for packet/tunnel (no inline geometry literals)."
    - "CATALOG_VIZ_MODULE_IDS includes all keys eventually covered across 22-01 and 22-02."
    - "Existing viz-packet-tunnel-modules tests pass after R3F refactor."
  artifacts:
    - path: "src/client/viz/viz-mesh-spec.ts"
      provides: "Shared geometry SOT for packet and tunnel modules."
    - path: "tests/viz-mesh-spec.test.ts"
      provides: "Spec key coverage and geometry constant tests."
    - path: "tests/headless-scene-parity.test.ts"
      provides: "GL-gated headless mesh tests (packet/tunnel subset)."
  key_links:
    - from: "viz-mesh-spec.ts"
      to: "src/client/viz/style-tokens.ts"
      via: "Token key references for colors and emissive"
      pattern: "STYLE_TOKENS"
    - from: "build-viz-three-scene.ts"
      to: "viz-mesh-spec.ts"
      via: "createVizModuleMesh dispatch"
      pattern: "VIZ_MESH_SPEC"
    - from: "viz-packet-flow.tsx"
      to: "viz-mesh-spec.ts"
      via: "PACKET_MESH_SPEC geometry args"
      pattern: "viz-mesh-spec"
---

<objective>
Deliver RENDER-02 foundation: shared mesh spec and factory for packet/tunnel modules, refactor R3F and headless builder to consume it.

Purpose: Eliminate duplicated geometry between R3F catalog and headless capture before cert/HUD parity in 22-02.
Output: `viz-mesh-spec.ts`, refactored packet/tunnel components, headless builder using factory.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-scene-builder-parity/22-CONTEXT.md
@.planning/phases/22-scene-builder-parity/22-RESEARCH.md
@.planning/phases/22-scene-builder-parity/22-PATTERNS.md
@src/client/viz/registry.ts
@src/client/viz/style-tokens.ts
@src/render/headless/build-viz-three-scene.ts
@src/client/viz/packet/viz-packet-flow.tsx
@src/client/viz/tunnel/viz-tunnel-secure.tsx
@tests/viz-packet-tunnel-modules.test.ts

<interfaces>
Mesh spec shape (introduce):
```typescript
export const PACKET_MESH_SPEC = {
  "viz-packet-flow": { radius: 0.15, colorToken: "colorAccentData", emissiveToken: "lightAccentGlowOpacity" },
  // ...
} as const;

export function createHeadlessModuleMeshes(
  moduleId: Phase18ModuleId,
  context: VizMeshBuildContext
): THREE.Object3D[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create viz-mesh-spec for packet and tunnel modules</name>
  <read_first>
    - src/client/viz/packet/*.tsx
    - src/client/viz/tunnel/*.tsx
    - src/client/viz/style-tokens.ts
  </read_first>
  <files>
    - src/client/viz/viz-mesh-spec.ts
    - tests/viz-mesh-spec.test.ts
  </files>
  <action>
    Create `viz-mesh-spec.ts` with `PACKET_MESH_SPEC`, `TUNNEL_MESH_SPEC` constants matching current R3F geometry (radii, torus args, wireframe flag, dimmed opacity 0.35). Export `createPacketMesh`, `createTunnelMesh` returning THREE.Object3D[] positioned from `VizPacketRenderState` when applicable. Add tests: spec keys match PACKET_MODULE_IDS + TUNNEL_MODULE_IDS; no raw hex strings in spec file (token keys only).
  </action>
  <acceptance_criteria>viz-mesh-spec.test.ts passes; five module ids defined.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-mesh-spec.test.ts</automated>
  </verify>
  <done>Packet/tunnel geometry SOT exists.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor R3F packet and tunnel components to import spec</name>
  <read_first>
    - src/client/viz/viz-mesh-spec.ts
    - tests/viz-packet-tunnel-modules.test.ts
  </read_first>
  <files>
    - src/client/viz/packet/viz-packet-flow.tsx
    - src/client/viz/packet/viz-packet-encrypted.tsx
    - src/client/viz/packet/viz-packet-threat.tsx
    - src/client/viz/tunnel/viz-tunnel-secure.tsx
    - src/client/viz/tunnel/viz-tunnel-handshake.tsx
  </files>
  <action>
    Replace inline geometry args with imports from viz-mesh-spec (e.g. `PACKET_MESH_SPEC["viz-packet-flow"].radius`). Keep JSX structure; only geometry dimensions and shared opacity constants move to spec. Do not change visual behavior.
  </action>
  <acceptance_criteria>viz-packet-tunnel-modules tests pass unchanged.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-packet-tunnel-modules.test.ts</automated>
  </verify>
  <done>R3F packet/tunnel consume shared spec.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor headless builder to use mesh factory</name>
  <read_first>
    - src/render/headless/build-viz-three-scene.ts
    - src/client/viz/viz-mesh-spec.ts
  </read_first>
  <files>
    - src/render/headless/build-viz-three-scene.ts
    - tests/headless-scene-parity.test.ts
  </files>
  <action>
    Remove inline `addPacketSphere`/`addTunnelTorus` helpers. Dispatch packet/tunnel modules via `createHeadlessModuleMeshes` or equivalent from viz-mesh-spec. Add GL-gated test: golden-scene-spec frame 0 scene contains ≥1 mesh per active packet/tunnel module in renderOrder. Use same skipIf pattern as headless-capture.test.ts.
  </action>
  <acceptance_criteria>headless builder has no packet/tunnel geometry literals; headless-scene-parity tests pass when GL available.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/headless-scene-parity.test.ts tests/headless-capture.test.ts</automated>
  </verify>
  <done>Headless packet/tunnel use factory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R3F ↔ headless | Shared spec must not break React component render |
| GL tests | skipIf when headless renderer unavailable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | T | Geometry drift | mitigate | Single viz-mesh-spec SOT + viz tests |
| T-22-02 | D | R3F regression | mitigate | Run viz-packet-tunnel-modules in Task 2 verify |
</threat_model>

<verification>
- [ ] `npm run test -- tests/viz-mesh-spec.test.ts`
- [ ] `npm run test -- tests/viz-packet-tunnel-modules.test.ts`
- [ ] `npm run test -- tests/headless-scene-parity.test.ts`
</verification>

<success_criteria>
- Packet and tunnel modules share geometry SOT between R3F and headless builder.
</success_criteria>

<output>
After completion, create `.planning/phases/22-scene-builder-parity/22-01-SUMMARY.md`
</output>
