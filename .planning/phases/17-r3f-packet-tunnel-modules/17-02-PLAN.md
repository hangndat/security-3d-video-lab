---
phase: 17-r3f-packet-tunnel-modules
plan: 02
type: execute
wave: 1
depends_on:
  - 17-01
files_modified:
  - src/client/viz/resolve-modules.ts
  - src/client/viz/tunnel/viz-tunnel-secure.tsx
  - src/client/viz/tunnel/viz-tunnel-handshake.tsx
  - src/client/viz/compose-scene.tsx
  - src/client/viz/registry.ts
  - src/client/viz/build-viz-frame-state.ts
  - tests/viz-packet-tunnel-modules.test.ts
autonomous: true
requirements:
  - VIZ-02
must_haves:
  truths:
    - "Tunnel modules viz-tunnel-secure and viz-tunnel-handshake registered and composed behind packets."
    - "resolve-modules.ts selects tunnel + packet stack from active timeline without breaking interpolation."
    - "compose-scene.tsx enforces catalog z-order (tunnel before packet)."
    - "Golden fixture frame ranges produce expected module id stacks."
    - "VIZ-02 tests pass alongside VIZ-01 suite."
  artifacts:
    - path: "src/client/viz/resolve-modules.ts"
      provides: "Active viz-* module stack per frame."
    - path: "src/client/viz/compose-scene.tsx"
      provides: "Z-ordered R3F scene composition."
    - path: "src/client/viz/tunnel/viz-tunnel-secure.tsx"
      provides: "Secure channel tunnel wrapper."
    - path: "tests/viz-packet-tunnel-modules.test.ts"
      provides: "VIZ-02 module stack and composition tests."
  key_links:
    - from: "resolve-modules.ts"
      to: "src/engine/timeline/scheduler.ts"
      via: "Active timeline window heuristics"
      pattern: "activeTimelineIds"
    - from: "compose-scene.tsx"
      to: "docs/r3f-module-catalog.md"
      via: "Z-order and max-primary rules"
      pattern: "tunnel → packet"
    - from: "viz-tunnel-handshake.tsx"
      to: "src/client/viz/style-tokens.ts"
      via: "Rim/data tokens for kex beats"
      pattern: "STYLE_TOKENS"
---

<objective>
Deliver VIZ-02: R3F tunnel modules composed with packet layer using timeline windows and catalog composition rules.

Purpose: Complete Phase 17 visual stack (packet + tunnel families) ready for cert/HUD in Phase 18.
Output: Tunnel components, module resolver, compose-scene, VIZ-02 tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-r3f-packet-tunnel-modules/17-CONTEXT.md
@.planning/phases/17-r3f-packet-tunnel-modules/17-RESEARCH.md
@docs/r3f-module-catalog.md
@src/client/viz/build-viz-frame-state.ts
@src/fixtures/golden-scene-spec.json
@src/content/topics/tls/contract.json

<interfaces>
Module stack resolver:
```typescript
export type VizModuleStack = {
  primary: string[]; // max 2: tunnel and/or packet ids
  zOrder: string[];  // back → front
};

export function resolveVizModuleStack(
  vizFrameState: VizFrameState,
  sceneSpec: SceneSpec
): VizModuleStack;
```

Tunnel activation heuristics (TLS reference):
- `viz-tunnel-handshake`: active during client/server hello timeline cues
- `viz-tunnel-secure`: active during finished / app-data cues
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module resolver and tunnel R3F components</name>
  <read_first>
    - docs/r3f-module-catalog.md (Tunnel Modules, Composition Rules)
    - src/content/topics/tls/contract.json (beat ids)
  </read_first>
  <files>
    - src/client/viz/resolve-modules.ts
    - src/client/viz/tunnel/viz-tunnel-secure.tsx
    - src/client/viz/tunnel/viz-tunnel-handshake.tsx
    - src/client/viz/registry.ts
  </files>
  <action>
    Implement resolveVizModuleStack using activeTimelineIds + packet module ids from VizFrameState. Enforce max 2 primary modules and no threat+encrypted same window (catalog rule).
    Create tunnel R3F components: environment wrapper (torus/cylinder tunnel mesh) using style tokens. Register viz-tunnel-secure and viz-tunnel-handshake in registry.ts.
  </action>
  <acceptance_criteria>Resolver returns deterministic stacks; tunnel ids registered.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-packet-tunnel-modules.test.ts --testNamePattern="resolveVizModuleStack|tunnel"</automated>
  </verify>
  <done>Tunnel modules and resolver complete.</done>
</task>

<task type="auto">
  <name>Task 2: Compose scene with z-order</name>
  <read_first>
    - src/client/viz/registry.ts
    - docs/r3f-module-catalog.md (Z-order table)
  </read_first>
  <files>
    - src/client/viz/compose-scene.tsx
    - src/client/viz/build-viz-frame-state.ts
  </files>
  <action>
    Create compose-scene.tsx exporting VizScene component: accepts sceneSpec + frame, calls buildVizFrameState + resolveVizModuleStack, renders modules in z-order (tunnel then packet). Extend VizFrameState or export compose helper for tests without requiring Canvas in CI if needed (export getComposePlan for pure test path).
  </action>
  <acceptance_criteria>Compose plan lists modules back-to-front matching catalog z-order.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-packet-tunnel-modules.test.ts --testNamePattern="compose"</automated>
  </verify>
  <done>End-to-end viz composition API for Phase 17 module families.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VIZ-02 integration tests</name>
  <read_first>
    - tests/viz-packet-tunnel-modules.test.ts
    - src/fixtures/golden-scene-spec.json
  </read_first>
  <files>
    - tests/viz-packet-tunnel-modules.test.ts
  </files>
  <behavior>
    - Test 1: resolveVizModuleStack deterministic on golden fixture frames 0, 15, 29.
    - Test 2: tunnel module id present when packet timeline cue active (handshake vs secure windows).
    - Test 3: packet interpolation positions unchanged when tunnel module added (compare VizFrameState packets before/after tunnel compose — positions identical).
    - Test 4: registry includes all five Phase 17 catalog ids (3 packet + 2 tunnel).
  </behavior>
  <action>Extend tests with describe "VIZ-02 tunnel modules".</action>
  <acceptance_criteria>VIZ-02 composition rules verified against golden fixture.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-packet-tunnel-modules.test.ts</automated>
  </verify>
  <done>Phase 17 full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver → compose | Read-only module stack |
| tunnel → packet | Tunnel must not mutate packet positions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | D | z-order inversion | mitigate | Test compose plan order |
| T-17-04 | E | extra primary modules | mitigate | Max-2 rule in resolver |
</threat_model>

<verification>
- [ ] `npm run test -- tests/viz-packet-tunnel-modules.test.ts`
- [ ] `npm run test -- tests/render-composition.test.ts`
- [ ] `npm run validate:requirements`
</verification>

<success_criteria>
- VIZ-02: Tunnel modules compose with packets per timeline windows without breaking interpolation.
</success_criteria>

<output>
After completion, create `.planning/phases/17-r3f-packet-tunnel-modules/17-02-SUMMARY.md`
</output>
