---
phase: 24-render-ci-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-3d-render.mjs
  - package.json
  - .github/workflows/ci.yml
  - docs/tls-crew-walkthrough.md
  - tests/render-ci-policy.test.ts
autonomous: true
requirements:
  - RENDER-04
must_haves:
  truths:
    - "verify:3d-render orchestrates headless-capture, scene-parity, and tls-3d-production quick gates."
    - "phase24/3d-render.json written with gateStatus pass and backendPolicy block."
    - "pr-full-validation sets SECURITY_LAB_RENDER_BACKEND=trace-hash and runs verify:3d-render --quick."
    - "pr-render-smoke sets SECURITY_LAB_RENDER_BACKEND=trace-hash."
    - "nightly-render-matrix runs verify:3d-render --quick."
    - "Documented local full 3D publish command in tls-crew-walkthrough."
  artifacts:
    - path: "scripts/verify-3d-render.mjs"
      provides: "RENDER-04 umbrella verification gate."
    - path: ".artifacts/verification/phase24/3d-render.json"
      provides: "Machine-readable Phase 24 RENDER-04 evidence."
  key_links:
    - from: "verify-3d-render.mjs"
      to: "scripts/verify-headless-capture.mjs"
      via: "Sub-gate orchestration"
      pattern: "headless-capture"
    - from: ".github/workflows/ci.yml"
      to: "resolve-production-render-backend.ts"
      via: "SECURITY_LAB_RENDER_BACKEND env"
      pattern: "trace-hash"
---

<objective>
Deliver RENDER-04: unified 3D render CI policy, umbrella verify gate, and workflow/documentation updates.

Purpose: Make v1.5 render backend strategy machine-enforced in CI while preserving local 3D publish path.
Output: verify:3d-render, CI updates, backend policy docs, policy tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-render-ci-milestone-close/24-CONTEXT.md
@.planning/phases/24-render-ci-milestone-close/24-RESEARCH.md
@scripts/verify-headless-capture.mjs
@scripts/verify-headless-scene-parity.mjs
@scripts/verify-tls-3d-production.mjs
@.github/workflows/ci.yml
@docs/tls-crew-walkthrough.md

<interfaces>
Verify JSON shape:
```typescript
{
  generatedAt: string;
  gateStatus: "pass" | "fail";
  quickMode: boolean;
  backendPolicy: {
    ci: "trace-hash";
    localDefault: "r3f-headless";
    documentedLocal3dCommand: string;
  };
  blockingCriteria: {
    headlessCapturePassed: boolean;
    sceneParityPassed: boolean;
    tls3dProductionPassed: boolean;
  };
  suites: Array<{ label: string; passed: boolean }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify-3d-render umbrella gate</name>
  <read_first>
    - scripts/verify-headless-capture.mjs
    - scripts/verify-tls-3d-production.mjs
  </read_first>
  <files>
    - scripts/verify-3d-render.mjs
    - package.json
    - tests/render-ci-policy.test.ts
  </files>
  <action>
    Create verify-3d-render.mjs: spawn verify:headless-capture, verify:headless-scene-parity, verify:tls-3d-production each with --quick; aggregate results to `.artifacts/verification/phase24/3d-render.json` with backendPolicy block. Add npm script verify:3d-render. Add test asserting resolveProductionRenderBackend({}) === r3f-headless and trace-hash env mapping (can extend headless-capture tests or new render-ci-policy.test.ts).
  </action>
  <acceptance_criteria>node scripts/verify-3d-render.mjs --quick exits 0.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-3d-render.mjs --quick</automated>
  </verify>
  <done>Umbrella RENDER-04 gate operational.</done>
</task>

<task type="auto">
  <name>Task 2: CI workflow backend policy</name>
  <read_first>
    - .github/workflows/ci.yml
  </read_first>
  <files>
    - .github/workflows/ci.yml
  </files>
  <action>
    Add SECURITY_LAB_RENDER_BACKEND=trace-hash to pr-render-smoke job env. Add verify:3d-render --quick to pr-full-validation verification evidence step (after verify:headless-capture). Add verify:3d-render --quick to nightly-render-matrix after full test suite or before upload. Optional: add nightly-3d-tls job on macos-latest running tls-production 3D GL test only with continue-on-error — include only if RESEARCH recommends and stays minimal.
  </action>
  <acceptance_criteria>CI yaml validates; pr jobs enforce trace-hash on render paths.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-3d-render.mjs --quick && npm run test -- tests/render-ci-policy.test.ts</automated>
  </verify>
  <done>CI reflects RENDER-04 backend policy.</done>
</task>

<task type="auto">
  <name>Task 3: Document local vs CI render profiles</name>
  <read_first>
    - docs/tls-crew-walkthrough.md
  </read_first>
  <files>
    - docs/tls-crew-walkthrough.md
  </files>
  <action>
    Add Render Backend Policy table: CI trace-hash, local r3f-headless default, env overrides. Document local full 3D publish: `generateTlsProductionArtifacts` with default env (or explicit npm script if added). Add verify:3d-render to Verification Commands. Note nightly uses trace-hash; macos/local for full PNG export.
  </action>
  <acceptance_criteria>Walkthrough documents both CI and local 3D paths.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/render-ci-policy.test.ts</automated>
  </verify>
  <done>RENDER-04 documented for operators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI ubuntu | No GL required for pass |
| Local dev | Full 3D optional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | D | CI 3D flake | mitigate | trace-hash + skipIf in sub-gates |
| T-24-02 | T | Policy drift | mitigate | render-ci-policy tests |
</threat_model>

<verification>
- [ ] `node scripts/verify-3d-render.mjs --quick`
- [ ] `npm run test -- tests/render-ci-policy.test.ts`
</verification>

<success_criteria>
- RENDER-04: CI trace-hash enforced; verify:3d-render PASS; local 3D path documented.
</success_criteria>

<output>
After completion, create `.planning/phases/24-render-ci-milestone-close/24-01-SUMMARY.md`
</output>
