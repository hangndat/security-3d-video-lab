---
phase: 06-narrative-cinematic-composition
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/content/composition/generate-caption-timing-map.ts
  - src/content/composition/caption-timing-map.schema.json
  - scripts/verify-narrative-composition.mjs
  - tests/caption-timing-map.test.ts
  - tests/narrative-composition-replay.test.ts
  - .github/workflows/ci.yml
  - .planning/phases/06-narrative-cinematic-composition/VERIFICATION.md
  - .artifacts/verification/phase06/narrative-composition.json
autonomous: true
requirements:
  - CINE-01
  - CINE-03
must_haves:
  truths:
    - "Stitched long-form compositions produce stable deterministic trace inputs across repeated runs."
    - "Caption timing maps list every storyboard beat with frame-accurate stitched offsets for long-form output."
    - "Phase 06 verification emits dual-format JSON and markdown evidence with blocking gate status."
  artifacts:
    - path: "src/content/composition/generate-caption-timing-map.ts"
      provides: "Pure caption map generator from assembly + topic contracts."
    - path: "src/content/composition/caption-timing-map.schema.json"
      provides: "Strict schema for caption timing artifacts."
    - path: ".artifacts/captions/network-foundations-long-v1.json"
      provides: "Canonical assembly caption map output."
    - path: "scripts/verify-narrative-composition.mjs"
      provides: "Phase 06 verification runner and evidence writer."
    - path: "tests/narrative-composition-replay.test.ts"
      provides: "Deterministic replay checks for stitched long-form scenes."
  key_links:
    - from: "generate-caption-timing-map.ts"
      to: "build-long-form-scene-spec.ts"
      via: "Shared frame offset walk aligned with stitch order"
      pattern: "frameOffset|startFrame"
    - from: "tests/narrative-composition-replay.test.ts"
      to: "src/render/remotion/render-composition.tsx"
      via: "buildDeterministicTraceInputs on stitched SceneSpec"
      pattern: "buildDeterministicTraceInputs"
    - from: "scripts/verify-narrative-composition.mjs"
      to: ".github/workflows/ci.yml"
      via: "PR validation invokes phase 06 test suites"
      pattern: "narrative-composition|caption-timing"
---

<objective>
Add beat-level caption timing maps and deterministic replay verification for assembly-driven long-form compositions, with blocking phase evidence.

Purpose: Close CINE-01 and CINE-03 — cinematic stability via replay checks and narration-ready caption timing per beat.
Output: Caption generator, schemas, replay tests, verify script, CI wiring, dual-format verification artifacts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-narrative-cinematic-composition/06-CONTEXT.md
@.planning/phases/06-narrative-cinematic-composition/06-01-PLAN.md
@tests/e2e-canonical-flows.test.ts
@src/render/remotion/render-composition.tsx

<interfaces>
From 06-01:
- `loadLongFormAssembly(slug)` returns assembly profile.
- `buildLongFormSceneSpec(assemblySlug, topicScenes)` returns stitched SceneSpec.

From E2E tests:
- `TRACE_FRAMES` constant pattern for deterministic sampling.
- Trace equality assertion across duplicate runs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement caption timing map generator and schema</name>
  <read_first>
    - .planning/phases/06-narrative-cinematic-composition/06-CONTEXT.md (D-11, D-12, D-13)
    - src/content/batch/first-content-batch.ts
    - src/content/composition/build-long-form-scene-spec.ts
  </read_first>
  <files>
    - src/content/composition/caption-timing-map.schema.json
    - src/content/composition/generate-caption-timing-map.ts
    - tests/caption-timing-map.test.ts
  </files>
  <behavior>
    - Test 1: canonical assembly caption map includes every beat from tls, ssh, dns with monotonic frame ranges.
    - Test 2: stitched offsets match manual calculation for second topic (frameOffset > 0).
    - Test 3: schema validation rejects missing scriptIntent or non-monotonic endFrame.
    - Test 4: expansion assembly map entry count equals sum of beats across six topics.
  </behavior>
  <action>Implement pure generator walking assembly sequence, accumulating frame offsets identically to stitch logic. Map narration placeholders to caption entries with seconds at FPS=30. Write optional CLI helper or test fixture writer to `.artifacts/captions/<slug>.json`.</action>
  <acceptance_criteria>Caption maps are deterministic, schema-valid, and cover 100% of storyboard beats in target assemblies.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/caption-timing-map.test.ts</automated>
  </verify>
  <done>Long-form outputs have machine-readable per-beat caption timing artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic replay tests for stitched long-form scenes</name>
  <read_first>
    - tests/e2e-canonical-flows.test.ts
    - tests/first-content-batch-export.test.ts
  </read_first>
  <files>
    - tests/narrative-composition-replay.test.ts
  </files>
  <behavior>
    - Test 1: canonical assembly stitched scene produces identical trace inputs on two consecutive runs.
    - Test 2: trace inputs change when a topic scene seed changes (sensitivity check).
    - Test 3: expansion assembly stitched scene passes replay equality at sampled frames.
  </behavior>
  <action>Implement D-09 using same frame sample strategy as E2E canonical flows. Use fixture scene specs for all topics in each assembly. Assert `buildDeterministicTraceInputs` stability; optionally assert fingerprint inequality on seed mutation.</action>
  <acceptance_criteria>Replay tests pass locally and fail clearly when stitch or scheduler behavior drifts.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/narrative-composition-replay.test.ts</automated>
  </verify>
  <done>Long-form cinematic composition is guarded by deterministic replay checks (CINE-01).</done>
</task>

<task type="auto">
  <name>Task 3: Phase verification script, CI wiring, and dual evidence</name>
  <read_first>
    - scripts/verify-content-authoring.mjs
    - .github/workflows/ci.yml
  </read_first>
  <files>
    - scripts/verify-narrative-composition.mjs
    - .github/workflows/ci.yml
    - .planning/phases/06-narrative-cinematic-composition/VERIFICATION.md
    - .artifacts/verification/phase06/narrative-composition.json
  </files>
  <behavior>
    - Verification JSON includes gateStatus, assembly coverage, caption entry counts, replay suite results.
    - Markdown mirrors JSON gate status and suite table.
    - `--quick` skips E2E smoke but marks suite as SKIP (does not fake PASS).
  </behavior>
  <action>Implement D-14, D-15. Script runs long-form-assembly, caption-timing-map, and narrative-composition-replay tests; writes JSON + VERIFICATION.md. Update CI pr-full-validation job to include phase 06 test files. Document expansion caption artifact paths.</action>
  <acceptance_criteria>verify script exits 0 on green tree; CI job references new tests; evidence files committed or generated in verify step per phase 05 pattern.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-narrative-composition.mjs --quick</automated>
  </verify>
  <done>Phase 06 has auditable dual-format verification matching project gate conventions.</done>
</task>

</tasks>

<verification>
Before marking plan complete:
- [ ] `npm run test -- tests/caption-timing-map.test.ts tests/narrative-composition-replay.test.ts tests/long-form-assembly.test.ts`
- [ ] `node scripts/verify-narrative-composition.mjs --quick`
- [ ] Full gate (optional local): `node scripts/verify-narrative-composition.mjs` includes replay + contract suites
</verification>

<threats>
| ID | Threat | Mitigation |
|----|--------|------------|
| T-06-04 | Caption/stitch frame drift | Shared offset helper or cross-test golden offsets |
| T-06-05 | Quick verify masking failures | SKIP status for skipped suites (Phase 05 lesson) |
| T-06-06 | CI time increase | Scope replay tests to sampled frames only |
</threats>
