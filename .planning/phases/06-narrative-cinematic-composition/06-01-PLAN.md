---
phase: 06-narrative-cinematic-composition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/assemblies/long-form-assembly.schema.json
  - src/content/assemblies/network-foundations-long-v1.json
  - src/content/assemblies/security-expansion-long-v1.json
  - src/content/composition/load-long-form-assembly.ts
  - src/content/composition/validate-long-form-assembly.ts
  - src/content/composition/build-long-form-scene-spec.ts
  - src/content/composition/pacing-presets.ts
  - src/content/contracts/transition-presets.ts
  - src/content/topics/dns/contract.json
  - src/content/topics/auth-session/contract.json
  - src/content/topics/pki-trust-chain/contract.json
  - src/content/batch/first-content-batch.ts
  - tests/long-form-assembly.test.ts
autonomous: true
requirements:
  - CONT-02
  - AUTHR-03
must_haves:
  truths:
    - "Long-form output can be built from a JSON assembly profile without hardcoded tls/ssh/dns-only logic."
    - "Transition and pacing presets are reusable catalogs validated for each adjacent topic pair in an assembly."
    - "Canonical network-foundations-long-v1 stitch behavior remains backward compatible with phase 03 export tests."
  artifacts:
    - path: "src/content/assemblies/network-foundations-long-v1.json"
      provides: "Canonical 3-topic assembly profile (TLS→SSH→DNS)."
    - path: "src/content/assemblies/security-expansion-long-v1.json"
      provides: "6-topic expansion assembly profile using full manifest sequence."
    - path: "src/content/composition/build-long-form-scene-spec.ts"
      provides: "Assembly-driven stitch entrypoint replacing hardcoded sequence checks."
    - path: "src/content/composition/pacing-presets.ts"
      provides: "Reusable pacing preset registry for cinematic rhythm policy."
    - path: "tests/long-form-assembly.test.ts"
      provides: "Automated validation for assembly profiles, preset compatibility, and stitch coherence."
  key_links:
    - from: "src/content/composition/load-long-form-assembly.ts"
      to: "src/content/composition/validate-long-form-assembly.ts"
      via: "Loaded assembly validated against manifest + contracts"
      pattern: "validateLongFormAssembly\\("
    - from: "src/content/composition/build-long-form-scene-spec.ts"
      to: "src/render/remotion/render-composition.tsx"
      via: "stitchSceneSpecsInOrder with topic-prefixed timeline offsets"
      pattern: "stitchSceneSpecsInOrder"
    - from: "src/content/contracts/transition-presets.ts"
      to: "src/content/topics/*/contract.json"
      via: "transitionToNext.presetId whitelist + allowedPairs"
      pattern: "validateTransitionPresetPair"
---

<objective>
Deliver data-driven long-form assembly profiles and expanded transition/pacing preset catalogs so multi-module narratives can be configured without core renderer edits.

Purpose: Satisfy CONT-02 and AUTHR-03 foundations — configurable sequences with explicit transition rationale and reusable presets.
Output: Assembly JSON schema, two assembly profiles, composition loader/validator/builder modules, extended transition presets, and regression tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-narrative-cinematic-composition/06-CONTEXT.md
@.planning/phases/06-narrative-cinematic-composition/06-RESEARCH.md
@.planning/phases/06-narrative-cinematic-composition/PATTERNS.md
@.planning/phases/05-content-authoring-foundation/05-01-SUMMARY.md
@src/content/batch/first-content-batch.ts
@src/content/contracts/transition-presets.ts
@tests/first-content-batch-export.test.ts

<interfaces>
From `first-content-batch.ts`:
- `LongFormAssembly` shape: `slug`, `sequence`, `transitions[]`, `targetWindowMinutes`.
- `validateLongFormTransitionCoherence()` expects transitions for each adjacent pair in sequence.
- `buildLongFormSceneSpec(topicScenes)` calls `stitchSceneSpecsInOrder`.

From Phase 05:
- `loadTopicContracts()` + `loadTopicManifest()` provide topic order and transition metadata.
- Transition preset IDs must exist in `TRANSITION_PRESETS` with matching `allowedPairs`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define long-form assembly schema and canonical profiles</name>
  <read_first>
    - .planning/phases/06-narrative-cinematic-composition/06-CONTEXT.md
    - src/content/topics/manifest.json
    - src/content/batch/first-content-batch.ts
  </read_first>
  <files>
    - src/content/assemblies/long-form-assembly.schema.json
    - src/content/assemblies/network-foundations-long-v1.json
    - src/content/assemblies/security-expansion-long-v1.json
    - src/content/composition/load-long-form-assembly.ts
    - src/content/composition/validate-long-form-assembly.ts
  </files>
  <behavior>
    - Test 1: schema rejects missing slug, empty sequence, or unsupported schemaVersion.
    - Test 2: validator rejects sequence topics not present in manifest order.
    - Test 3: validator rejects duplicate topics in assembly sequence.
    - Test 4: canonical profile `network-foundations-long-v1` matches tls,ssh,dns sequence and 4-6 minute window.
  </behavior>
  <action>Implement D-01, D-02, D-03, D-04 with JSON schema for assembly profiles and loader/validator modules. Create canonical 3-topic and 6-topic expansion profiles. Validator must collect all errors (missing topics, ordering violations, window bounds) in one pass.</action>
  <acceptance_criteria>Both assembly JSON files validate; loader returns typed assembly objects; validator produces deterministic issue paths.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/long-form-assembly.test.ts --testNamePattern="schema|manifest|canonical"</automated>
  </verify>
  <done>Assembly profiles are loadable and validated against manifest lock without hardcoded topic lists in code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand transition/pacing presets and wire topic transitions for expansion chain</name>
  <read_first>
    - src/content/contracts/transition-presets.ts
    - src/content/topics/dns/contract.json
    - src/content/topics/auth-session/contract.json
  </read_first>
  <files>
    - src/content/composition/pacing-presets.ts
    - src/content/contracts/transition-presets.ts
    - src/content/topics/dns/contract.json
    - src/content/topics/auth-session/contract.json
    - src/content/topics/pki-trust-chain/contract.json
  </files>
  <behavior>
    - Test 1: unknown transition preset ID fails validation.
    - Test 2: expansion chain presets allow dns→auth-session, auth-session→pki-trust-chain, pki-trust-chain→mitm-defense.
    - Test 3: pacing preset registry exposes at least `documentary-standard` and validates unknown IDs.
  </behavior>
  <action>Implement D-05, D-06, D-07. Add transition presets for expansion adjacencies with human-readable labels and rationale-compatible pair rules. Add `transitionToNext` on dns, auth-session, pki-trust-chain contracts. Introduce pacing preset catalog and optional `pacingPresetId` validation hook (warn if missing, fail if unknown).</action>
  <acceptance_criteria>Full 6-topic contract set validates with complete transition chain; pacing presets are importable without render dependencies.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/long-form-assembly.test.ts --testNamePattern="preset|transition|pacing"</automated>
  </verify>
  <done>Reusable transition and pacing presets cover both canonical and expansion assemblies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generalize long-form stitch pipeline and preserve phase 03 compatibility</name>
  <read_first>
    - src/content/composition/validate-long-form-assembly.ts
    - src/render/remotion/render-composition.tsx
    - tests/first-content-batch-export.test.ts
  </read_first>
  <files>
    - src/content/composition/build-long-form-scene-spec.ts
    - src/content/batch/first-content-batch.ts
    - tests/long-form-assembly.test.ts
  </files>
  <behavior>
    - Test 1: `buildLongFormSceneSpec('network-foundations-long-v1', scenes)` produces same sceneId/seed/timeline length as legacy path.
    - Test 2: `validateLongFormTransitionCoherence` works for expansion assembly with 5 transitions.
    - Test 3: missing scene for required topic throws clear error before stitch.
  </behavior>
  <action>Implement D-08, D-10. Move stitch/coherence logic to composition module. Refactor `first-content-batch.ts` to delegate to assembly loader (re-export legacy constants for tests). Remove hardcoded `tls,ssh,dns` assertion; derive expected pairs from assembly sequence. Run existing export test unchanged.</action>
  <acceptance_criteria>Phase 03 export test passes; expansion assembly builds stitched SceneSpec when all topic scenes provided.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/first-content-batch-export.test.ts tests/long-form-assembly.test.ts</automated>
  </verify>
  <done>Long-form stitch is assembly-driven while canonical 3-topic behavior is preserved.</done>
</task>

</tasks>

<verification>
Before marking plan complete:
- [ ] `npm run test -- tests/long-form-assembly.test.ts tests/first-content-batch-export.test.ts tests/first-content-batch.test.ts`
- [ ] `npm run test -- tests/content-contracts.test.ts` (no regressions on topic validation)
</verification>

<threats>
| ID | Threat | Mitigation |
|----|--------|------------|
| T-06-01 | Breaking canonical long-form seed/timeline | Golden comparison test in Task 3 |
| T-06-02 | Incomplete expansion transitions | Validator fails on missing adjacent preset before stitch |
| T-06-03 | Circular import composition↔batch | composition module must not import first-content-batch |
</threats>
