---
phase: 09-advanced-security-topics
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/content/topics/zero-trust-access/contract.json
  - src/content/topics/oauth-jwt-session/contract.json
  - src/content/topics/api-gateway-waf/contract.json
  - src/content/topics/manifest.json
  - src/content/assemblies/content-depth-long-v1.json
  - src/content/contracts/transition-presets.ts
  - tests/content-authoring-foundation.test.ts
  - tests/expansion-module-e2e.test.ts
  - tests/long-form-assembly.test.ts
  - src/verification/module-packet.ts
  - scripts/verify-content-authoring.mjs
autonomous: true
requirements:
  - CONT-04
  - CONT-05
  - CONT-06
must_haves:
  truths:
    - "Three new security modules validate as topic contracts without core renderer edits."
    - "manifest.json lists nine topics in deterministic chain order."
    - "content-depth-long-v1 assembly validates with full transition coherence."
    - "Every new module has beat-level narration placeholder coverage enforced by CI."
  artifacts:
    - path: "src/content/topics/zero-trust-access/contract.json"
      provides: "Zero-trust access boundary explainer module."
    - path: "src/content/topics/oauth-jwt-session/contract.json"
      provides: "OAuth/JWT session flow explainer module."
    - path: "src/content/topics/api-gateway-waf/contract.json"
      provides: "API gateway and WAF edge defense module (terminal)."
    - path: "src/content/topics/manifest.json"
      provides: "Nine-topic manifest lock."
    - path: "src/content/assemblies/content-depth-long-v1.json"
      provides: "Long-form profile stitching all v1.2 modules."
  key_links:
    - from: "src/content/topics/manifest.json"
      to: "src/content/contracts/load-topic-contracts.ts"
      via: "Manifest order drives contract loading"
      pattern: "loadTopicManifest"
    - from: "src/content/assemblies/content-depth-long-v1.json"
      to: "src/content/composition/validate-long-form-assembly.ts"
      via: "Nine-topic sequence and transition chain validation"
      pattern: "content-depth-long-v1"
    - from: "tests/expansion-module-e2e.test.ts"
      to: "src/verification/module-packet.ts"
      via: "Module packet completeness for v1.2 topics"
      pattern: "evaluateModulePacket"
---

<objective>
Author three advanced security topic contracts, register them in the nine-topic manifest, and integrate them into a new long-form assembly profile with blocking CI validation.

Purpose: Deliver CONT-04, CONT-05, and CONT-06 by shipping production-ready topic contracts and assembly linkage on top of the 09-01 engine extension.
Output: Three authored modules, updated manifest, content-depth-long-v1 assembly, and extended test/verify gates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-advanced-security-topics/09-CONTEXT.md
@.planning/phases/09-advanced-security-topics/09-01-PLAN.md
@src/content/topics/auth-session/contract.json
@src/content/assemblies/security-expansion-long-v1.json
@scripts/scaffold-topic-contract.mjs

<interfaces>
Manifest order after Phase 09 (D-02):
`["tls","ssh","dns","auth-session","pki-trust-chain","mitm-defense","zero-trust-access","oauth-jwt-session","api-gateway-waf"]`

Assembly profile (D-07):
- slug: `content-depth-long-v1`
- sequence: same nine topics as manifest
- targetWindowMinutes: `{ "min": 10, "max": 14 }` (adjust within schema limits)
- defaultPacingPresetId: `documentary-standard`

Each new contract must have:
- slug `{topic}-short-v1`
- 5+ storyboardBeats with non-overlapping frame windows
- narrationPlaceholders 1:1 with beats (CONT-06)
- pacingPresetId: `documentary-standard`
- transitionToNext on zero-trust-access and oauth-jwt-session; api-gateway-waf terminal

Scaffold command: `node scripts/scaffold-topic-contract.mjs --topic <slug> --major 1`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author three v1.2 topic contracts via scaffold + expansion</name>
  <read_first>
    - .planning/phases/09-advanced-security-topics/09-CONTEXT.md
    - src/content/topics/auth-session/contract.json
    - scripts/scaffold-topic-contract.mjs
    - src/content/contracts/validate-topic-contracts.ts
  </read_first>
  <files>
    - src/content/topics/zero-trust-access/contract.json
    - src/content/topics/oauth-jwt-session/contract.json
    - src/content/topics/api-gateway-waf/contract.json
    - src/content/contracts/transition-presets.ts
    - tests/content-authoring-foundation.test.ts
  </files>
  <behavior>
    - Test 1: each v1.2 contract passes validateTopicContracts with zero blocking errors.
    - Test 2: each contract has narrationPlaceholders count equal to storyboardBeats count.
    - Test 3: zero-trust-access and oauth-jwt-session have valid transitionToNext; api-gateway-waf has none.
  </behavior>
  <action>Scaffold then expand contracts for zero-trust-access, oauth-jwt-session, and api-gateway-waf. Match auth-session beat depth (5 beats minimum) with domain-specific objectives per 09-CONTEXT specifics. Wire transitionToNext: zero-trust-access → oauth-jwt-session (preset zero-trust-access-to-oauth-jwt-session), oauth-jwt-session → api-gateway-waf (preset oauth-jwt-session-to-api-gateway-waf). Ensure remaining two presets exist in transition-presets.ts if not fully added in 09-01. Update content-authoring-foundation.test.ts draft module lists to include all three v1.2 slugs.</action>
  <acceptance_criteria>Three contract.json files exist under src/content/topics/; each validates with zero errors; placeholder coverage is 1:1; CONT-04 satisfied without renderer source edits.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/content-authoring-foundation.test.ts tests/content-contracts.test.ts --testNamePattern="zero-trust|oauth|api-gateway|placeholder|draft"</automated>
  </verify>
  <done>Three advanced security modules exist as validated topic contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register nine-topic manifest and content-depth-long-v1 assembly</name>
  <read_first>
    - src/content/topics/manifest.json
    - src/content/assemblies/security-expansion-long-v1.json
    - src/content/composition/validate-long-form-assembly.ts
    - tests/long-form-assembly.test.ts
  </read_first>
  <files>
    - src/content/topics/manifest.json
    - src/content/assemblies/content-depth-long-v1.json
    - tests/long-form-assembly.test.ts
    - src/verification/module-packet.ts
    - tests/expansion-module-e2e.test.ts
  </files>
  <behavior>
    - Test 1: manifest.json order has nine unique slugs ending with v1.2 topics.
    - Test 2: content-depth-long-v1 validates with validateLongFormAssemblyProfile (zero errors).
    - Test 3: evaluateModulePacket succeeds for each v1.2 topic against content-depth-long-v1 linkage.
  </behavior>
  <action>Append zero-trust-access, oauth-jwt-session, api-gateway-waf to manifest.json order per D-02. Create content-depth-long-v1.json assembly with full nine-topic sequence per D-07. Update long-form-assembly.test.ts with profile validation and transition chain assertions. Extend module-packet.ts and expansion-module-e2e.test.ts to evaluate v1.2 topics (update hardcoded draft ID lists and long-form profile reference from security-expansion-long-v1 to content-depth-long-v1 where appropriate for v1.2 module checks).</action>
  <acceptance_criteria>manifest.json lists nine topics; content-depth-long-v1 passes assembly validator; module packet tests cover all three v1.2 slugs; CONT-05 satisfied.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/long-form-assembly.test.ts tests/expansion-module-e2e.test.ts --testNamePattern="content-depth|manifest|module"</automated>
  </verify>
  <done>Nine-topic manifest and long-form assembly integrate v1.2 modules with validated transitions.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh content-authoring verification evidence</name>
  <read_first>
    - scripts/verify-content-authoring.mjs
    - .planning/phases/05-content-authoring-foundation/05-02-PLAN.md
    - .github/workflows/ci.yml
  </read_first>
  <files>
    - scripts/verify-content-authoring.mjs
    - tests/content-authoring-foundation.test.ts
  </files>
  <behavior>
    - verify-content-authoring.mjs exits 0 after Phase 09 changes.
    - Draft module ID list in verify script includes v1.2 topics.
  </behavior>
  <action>Update verify-content-authoring.mjs module/topic ID lists to include v1.2 slugs and assert nine-topic manifest lock. Run verify script in --quick mode and ensure content-authoring-foundation tests assert nine draft/expansion modules validate. No CI workflow structural changes required — existing content gates cover new tests.</action>
  <acceptance_criteria>`node scripts/verify-content-authoring.mjs --quick` exits 0; verify script references nine-topic inventory; content-authoring-foundation.test.ts passes.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-content-authoring.mjs --quick && npm run test -- tests/content-authoring-foundation.test.ts</automated>
  </verify>
  <done>Phase 09 content authoring evidence is regenerable and CI-compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored JSON contracts → manifest lock | Only manifest-listed topics enter long-form assembly |
| assembly profile → composition pipeline | Sequence order enforced before stitch/render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | T | src/content/topics/*/contract.json | mitigate | Schema + collect-all validator before assembly consumption. |
| T-09-05 | I | manifest.json | mitigate | Manifest lock rejects undeclared topic order drift at validation. |
| T-09-06 | E | content-depth-long-v1.json | mitigate | validateLongFormAssemblyProfile enforces transition coherence per adjacent pair. |
</threat_model>

<verification>
- [ ] `npm run test -- tests/content-authoring-foundation.test.ts tests/content-contracts.test.ts tests/long-form-assembly.test.ts tests/expansion-module-e2e.test.ts`
- [ ] `node scripts/verify-content-authoring.mjs --quick`
</verification>

<success_criteria>
- CONT-04: three new modules ship via topic contracts without renderer edits.
- CONT-05: manifest + content-depth-long-v1 assembly include all v1.2 modules.
- CONT-06: narration placeholder coverage enforced for every new module in CI.
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-security-topics/09-02-SUMMARY.md`
</output>
