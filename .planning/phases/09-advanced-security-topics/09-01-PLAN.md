---
phase: 09-advanced-security-topics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/contracts/topic-contract.schema.json
  - src/content/contracts/types.ts
  - src/content/contracts/transition-presets.ts
  - src/content/topics/mitm-defense/contract.json
  - tests/content-contracts.test.ts
  - tests/long-form-assembly.test.ts
autonomous: true
requirements:
  - CONT-06
must_haves:
  truths:
    - "Validation engine accepts nine topic IDs and eight transition presets without schema version bump."
    - "mitm-defense links forward to zero-trust-access with a whitelisted preset pair."
    - "Contract and assembly tests fail on missing preset or enum regression."
  artifacts:
    - path: "src/content/contracts/topic-contract.schema.json"
      provides: "Extended topic and toTopic enums for v1.2 modules."
    - path: "src/content/contracts/transition-presets.ts"
      provides: "Three new v1.2 transition presets plus mitm-defense outbound pair."
    - path: "src/content/topics/mitm-defense/contract.json"
      provides: "Non-terminal mitm-defense with transitionToNext to zero-trust-access."
  key_links:
    - from: "src/content/contracts/transition-presets.ts"
      to: "src/content/contracts/validate-topic-contracts.ts"
      via: "validateTransitionPresetPair enforces new allowedPairs"
      pattern: "mitm-defense-to-zero-trust-access"
    - from: "tests/long-form-assembly.test.ts"
      to: "src/content/composition/validate-long-form-assembly.ts"
      via: "Preset chain regression for v1.2 tail"
      pattern: "validateLongFormAssemblyProfile"
---

<objective>
Extend the v1.1 contract engine to support three upcoming advanced security topics and the transition chain from mitm-defense through api-gateway-waf.

Purpose: Unblock Phase 09 content authoring by updating schema enums, type unions, preset catalog, and mitm-defense outbound transition before new contracts land.
Output: Nine-topic validation surface, eight-preset chain head, and regression tests proving engine readiness (CONT-06 infrastructure).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-advanced-security-topics/09-CONTEXT.md
@.planning/phases/09-advanced-security-topics/09-RESEARCH.md
@.planning/phases/09-advanced-security-topics/09-PATTERNS.md
@src/content/contracts/topic-contract.schema.json
@src/content/contracts/transition-presets.ts
@src/content/topics/mitm-defense/contract.json

<interfaces>
New v1.2 topic IDs (D-01): `zero-trust-access`, `oauth-jwt-session`, `api-gateway-waf`.

New transition preset IDs (D-09):
- `mitm-defense-to-zero-trust-access` (mitm-defense → zero-trust-access)
- `zero-trust-access-to-oauth-jwt-session`
- `oauth-jwt-session-to-api-gateway-waf`

Schema enum locations: `topic` property enum AND `transitionToNext.toTopic` enum in topic-contract.schema.json.

types.ts: extend topic ID constants so TopicId includes all nine slugs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend schema enums and TopicId unions for v1.2 topics</name>
  <read_first>
    - .planning/phases/09-advanced-security-topics/09-CONTEXT.md
    - src/content/contracts/topic-contract.schema.json
    - src/content/contracts/types.ts
    - tests/content-contracts.test.ts
  </read_first>
  <files>
    - src/content/contracts/topic-contract.schema.json
    - src/content/contracts/types.ts
    - tests/content-contracts.test.ts
  </files>
  <behavior>
    - Test 1: schema accepts contract with topic `zero-trust-access` when all other fields valid.
    - Test 2: schema rejects unknown topic slug not in enum.
    - Test 3: types.ts TopicId union includes all nine manifest slugs.
  </behavior>
  <action>Add `zero-trust-access`, `oauth-jwt-session`, and `api-gateway-waf` to both topic enums in topic-contract.schema.json. In types.ts, add `V12_TOPIC_IDS` constant array with the three new slugs and extend `TopicId` to include them (keep FIRST_CONTENT_BATCH_TOPICS and DRAFT_TOPIC_IDS for backward compatibility or consolidate into MANIFEST_TOPIC_IDS if cleaner). Add regression test asserting enum coverage for v1.2 slugs.</action>
  <acceptance_criteria>topic-contract.schema.json lists nine topics in both enum arrays; types.ts exports TopicId covering all nine slugs; content-contracts.test.ts passes enum acceptance and rejection cases.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/content-contracts.test.ts --testNamePattern="enum|topic|schema"</automated>
  </verify>
  <done>Validation engine recognizes v1.2 topic IDs before any new contract files exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add v1.2 transition presets and mitm-defense outbound link</name>
  <read_first>
    - src/content/contracts/transition-presets.ts
    - src/content/topics/mitm-defense/contract.json
    - src/content/topics/auth-session/contract.json
    - tests/content-contracts.test.ts
  </read_first>
  <files>
    - src/content/contracts/transition-presets.ts
    - src/content/topics/mitm-defense/contract.json
    - tests/content-contracts.test.ts
    - tests/long-form-assembly.test.ts
  </files>
  <behavior>
    - Test 1: validateTransitionPresetPair accepts mitm-defense → zero-trust-access with new preset ID.
    - Test 2: mitm-defense contract validates with transitionToNext to zero-trust-access.
    - Test 3: unknown preset ID for v1.2 pair fails validation.
  </behavior>
  <action>Register three presets in transition-presets.ts with allowedPairs per D-09. Append preset IDs to REQUIRED_TRANSITION_PRESET_IDS. Update mitm-defense/contract.json with transitionToNext: toTopic zero-trust-access, presetId mitm-defense-to-zero-trust-access, and rationale linking MITM defense to zero-trust access boundaries. Add tests for preset whitelist and pair validation covering the v1.2 tail presets (stub contracts acceptable in tests).</action>
  <acceptance_criteria>transition-presets.ts exports eight presets total; mitm-defense contract passes validateTopicContracts when loaded with updated chain; tests fail if any v1.2 preset ID is removed.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/content-contracts.test.ts tests/long-form-assembly.test.ts --testNamePattern="preset|transition|mitm"</automated>
  </verify>
  <done>Transition chain is continuous from mitm-defense through the v1.2 topic sequence at the engine layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preset catalog → contract validation | Whitelist prevents unauthorized cross-topic transitions |
| schema enum → loaded contracts | Only declared topic IDs enter batch/assembly pipelines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | E | transition-presets.ts | mitigate | Whitelist-only preset IDs with explicit allowedPairs per D-03/D-09. |
| T-09-02 | T | topic-contract.schema.json | mitigate | Strict enum blocks undeclared topic slugs before loader mapping. |
| T-09-03 | D | validate-topic-contracts.ts | mitigate | Collect-all validation returns deterministic issue arrays for CI gates. |
</threat_model>

<verification>
- [ ] `npm run test -- tests/content-contracts.test.ts tests/long-form-assembly.test.ts`
</verification>

<success_criteria>
- CONT-06 infrastructure ready: placeholder coverage validation applies to v1.2 topic enum entries.
- Engine accepts nine topics and eight transition presets with no schemaVersion change.
- mitm-defense is no longer terminal in the transition chain.
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-security-topics/09-01-SUMMARY.md`
</output>
