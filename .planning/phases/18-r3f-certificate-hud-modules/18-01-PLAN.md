---
phase: 18-r3f-certificate-hud-modules
plan: 01
type: execute
wave: 1
depends_on:
  - 17-02
files_modified:
  - src/client/viz/cert/viz-cert-single.tsx
  - src/client/viz/cert/viz-cert-chain.tsx
  - src/client/viz/resolve-modules.ts
  - src/client/viz/registry.ts
  - src/client/viz/style-tokens.ts
  - src/client/viz/build-viz-frame-state.ts
  - src/fixtures/tls-server-hello-scene-spec.json
  - tests/viz-cert-hud-modules.test.ts
autonomous: true
requirements:
  - VIZ-03
must_haves:
  truths:
    - "resolveCertModuleId selects viz-cert-single or viz-cert-chain from actors and active timeline."
    - "Certificate R3F components use STYLE_TOKENS.colorAccentTrust only (no raw hex)."
    - "Registry exports viz-cert-single and viz-cert-chain."
    - "tls-server-hello-scene-spec.json fixture drives cert module tests."
    - "VIZ-03 tests pass without WebGL mount."
  artifacts:
    - path: "src/client/viz/cert/viz-cert-single.tsx"
      provides: "Single server certificate visual."
    - path: "src/client/viz/cert/viz-cert-chain.tsx"
      provides: "Multi-link PKI chain visual."
    - path: "src/client/viz/resolve-modules.ts"
      provides: "Cert layer in module stack."
    - path: "src/fixtures/tls-server-hello-scene-spec.json"
      provides: "TLS server-hello beat fixture."
    - path: "tests/viz-cert-hud-modules.test.ts"
      provides: "VIZ-03 determinism and registry tests."
  key_links:
    - from: "resolve-modules.ts"
      to: "src/engine/camera/preset-registry.ts"
      via: "Intimate FOV reference for cert framing (read-only)"
      pattern: "fov.*35|intimate"
    - from: "viz-cert-single.tsx"
      to: "src/client/viz/style-tokens.ts"
      via: "Trust accent color"
      pattern: "colorAccentTrust"
    - from: "tls-server-hello-scene-spec.json"
      to: "docs/tls-crew-walkthrough.md"
      via: "tls-server-hello-beat id"
      pattern: "server-hello"
---

<objective>
Deliver VIZ-03: R3F certificate modules bound to SceneSpec actors and TLS server-hello timeline windows.

Purpose: Add trust-visual layer between packet/tunnel and HUD overlays (18-02).
Output: Two cert components, cert resolver extension, TLS server-hello fixture, VIZ-03 tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-r3f-certificate-hud-modules/18-CONTEXT.md
@.planning/phases/18-r3f-certificate-hud-modules/18-RESEARCH.md
@docs/r3f-module-catalog.md
@docs/style-bible.md
@src/client/viz/resolve-modules.ts
@src/client/viz/registry.ts
@src/client/viz/style-tokens.ts
@src/engine/camera/preset-registry.ts
@src/content/topics/tls/contract.json

<interfaces>
Cert module resolver (extend resolve-modules.ts):
```typescript
export type CertModuleId = "viz-cert-single" | "viz-cert-chain";

export function resolveCertModuleId(
  vizFrameState: VizFrameState,
  sceneSpec: SceneSpec
): CertModuleId | null;

// Extend VizModuleStack:
export type VizModuleStack = {
  primary: string[]; // max 2 non-HUD (tunnel, packet, cert)
  cert: CertModuleId | null;
  zOrder: string[];  // tunnel → packet → cert (HUD added in 18-02)
};
```

Cert component props:
```typescript
export type VizCertProps = {
  actors: SceneSpec["actors"];
  visible: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TLS server-hello fixture and style token extension</name>
  <read_first>
    - src/fixtures/golden-scene-spec.json
    - src/content/topics/tls/contract.json
    - docs/style-bible.md (typography section)
  </read_first>
  <files>
    - src/fixtures/tls-server-hello-scene-spec.json
    - src/client/viz/style-tokens.ts
  </files>
  <action>
    Create tls-server-hello-scene-spec.json: seed, sceneId, actors (client, server, optional ca/intermediate for chain test variant or second fixture inline in test), packets with server-hello route, timeline cue id containing "server-hello" active during tls-server-hello-beat frame range (54–98 per TLS contract reference).
    Extend style-tokens.ts with fontHudMd, fontHudSm, colorTextPrimary, colorTextMuted, cameraFovIntimate (35) mapped from style bible CSS vars.
  </action>
  <acceptance_criteria>Fixture validates against validateSceneSpec; typography tokens exported.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-cert-hud-modules.test.ts --testNamePattern="fixture|STYLE_TOKENS"</automated>
  </verify>
  <done>Server-hello fixture and extended tokens ready for cert modules.</done>
</task>

<task type="auto">
  <name>Task 2: Certificate R3F components and resolver</name>
  <read_first>
    - docs/r3f-module-catalog.md (Certificate Modules)
    - src/client/viz/tunnel/viz-tunnel-handshake.tsx (component pattern)
  </read_first>
  <files>
    - src/client/viz/cert/viz-cert-single.tsx
    - src/client/viz/cert/viz-cert-chain.tsx
    - src/client/viz/resolve-modules.ts
    - src/client/viz/registry.ts
    - src/client/viz/build-viz-frame-state.ts
  </files>
  <action>
    Implement resolveCertModuleId: return viz-cert-chain when sceneSpec.actors.length >= 3 OR actor labels suggest chain roles (ca/intermediate/leaf/server); return viz-cert-single when server-hello cue active or server actor present; null otherwise.
    Update resolveVizModuleStack to include cert in zOrder after packet, before HUD placeholder (cert slot only in 18-01). Enforce catalog max-2-primary rule: cert may replace packet slot when only cert+ tunnel active (document precedence: tunnel > cert > packet when over limit).
    Create VizCertSingle (single card mesh/plane) and VizCertChain (3 linked cards) using colorAccentTrust. Register CERT_MODULE_IDS in registry.ts.
  </action>
  <acceptance_criteria>Cert ids registered; resolver deterministic on server-hello frames.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-cert-hud-modules.test.ts --testNamePattern="resolveCert|cert"</automated>
  </verify>
  <done>Certificate module family implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VIZ-03 tests</name>
  <read_first>
    - tests/viz-packet-tunnel-modules.test.ts (patterns)
  </read_first>
  <files>
    - tests/viz-cert-hud-modules.test.ts
  </files>
  <behavior>
    - Test 1: resolveCertModuleId returns viz-cert-single on tls-server-hello fixture during server-hello window.
    - Test 2: resolveCertModuleId returns viz-cert-chain when ≥3 trust actors in scene spec.
    - Test 3: registry includes viz-cert-single and viz-cert-chain.
    - Test 4: cert component sources contain no hardcoded hex (regex scan).
    - Test 5: resolveVizModuleStack zOrder places cert after packet/tunnel.
    - Test 6: Phase 17 tests still pass (regression).
  </behavior>
  <action>Create tests/viz-cert-hud-modules.test.ts with describe "VIZ-03 certificate modules".</action>
  <acceptance_criteria>VIZ-03 structurally verifiable in CI.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-cert-hud-modules.test.ts --testNamePattern="VIZ-03"</automated>
  </verify>
  <done>VIZ-03 tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actors → cert | Read-only SceneSpec actor labels |
| cert → packet | Cert layer must not mutate packet positions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | I | cert module resolver | mitigate | Catalog ids only; tested heuristics |
| T-18-02 | D | z-order inversion | mitigate | zOrder test in Task 3 |
</threat_model>

<verification>
- [ ] `npm run test -- tests/viz-cert-hud-modules.test.ts --testNamePattern="VIZ-03"`
- [ ] `npm run test -- tests/viz-packet-tunnel-modules.test.ts`
</verification>

<success_criteria>
- VIZ-03: Certificate modules render from TLS server-hello fixtures with style bible trust tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/18-r3f-certificate-hud-modules/18-01-SUMMARY.md`
</output>
