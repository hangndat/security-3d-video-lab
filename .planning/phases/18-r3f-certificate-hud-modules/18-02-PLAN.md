---
phase: 18-r3f-certificate-hud-modules
plan: 02
type: execute
wave: 1
depends_on:
  - 18-01
files_modified:
  - src/client/viz/hud/viz-hud-actor-label.tsx
  - src/client/viz/hud/viz-hud-beat-caption.tsx
  - src/client/viz/hud/viz-hud-packet-id.tsx
  - src/client/viz/hud/viz-hud-frame-counter.tsx
  - src/client/viz/resolve-hud-caption.ts
  - src/client/viz/resolve-modules.ts
  - src/client/viz/compose-scene.tsx
  - src/client/viz/registry.ts
  - docs/r3f-module-catalog.md
  - tests/viz-cert-hud-modules.test.ts
autonomous: true
requirements:
  - VIZ-04
must_haves:
  truths:
    - "All four HUD catalog ids plus viz-hud-beat-caption registered (full HUD family)."
    - "resolveActiveCaption returns CaptionTimingEntry for current frame."
    - "HUD modules stack on top of cert/packet/tunnel per catalog z-order."
    - "getComposePlan accepts optional captionMap for beat-caption HUD."
    - "Full catalog viz-* ids (10 total) registered in VIZ_REGISTRY."
    - "VIZ-04 tests pass alongside VIZ-03 suite."
  artifacts:
    - path: "src/client/viz/resolve-hud-caption.ts"
      provides: "Active caption lookup by frame."
    - path: "src/client/viz/hud/viz-hud-beat-caption.tsx"
      provides: "Beat caption overlay."
    - path: "src/client/viz/compose-scene.tsx"
      provides: "Four-layer z-order composition."
    - path: "docs/r3f-module-catalog.md"
      provides: "viz-hud-beat-caption catalog row."
    - path: "tests/viz-cert-hud-modules.test.ts"
      provides: "VIZ-04 caption and HUD stack tests."
  key_links:
    - from: "resolve-hud-caption.ts"
      to: "src/content/composition/generate-caption-timing-map.ts"
      via: "CaptionTimingEntry beatId/scriptIntent"
      pattern: "resolveActiveCaption"
    - from: "compose-scene.tsx"
      to: "docs/r3f-module-catalog.md"
      via: "background → tunnel → packet → cert → HUD"
      pattern: "viz-hud-"
    - from: "viz-hud-actor-label.tsx"
      to: "src/client/viz/style-tokens.ts"
      via: "fontHudMd, colorTextPrimary"
      pattern: "fontHud"
---

<objective>
Deliver VIZ-04: R3F HUD modules with actor labels, beat captions from caption timing maps, and catalog-complete packet-id/frame-counter overlays.

Purpose: Complete the four-family R3F catalog; unblocks Phase 19 TLS production composition.
Output: Four HUD components, caption resolver, extended compose-scene, catalog doc update, VIZ-04 tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-r3f-certificate-hud-modules/18-CONTEXT.md
@.planning/phases/18-r3f-certificate-hud-modules/18-RESEARCH.md
@docs/r3f-module-catalog.md
@src/client/viz/compose-scene.tsx
@src/client/viz/resolve-modules.ts
@src/content/composition/generate-caption-timing-map.ts
@src/content/topics/tls/contract.json

<interfaces>
Caption resolver:
```typescript
import type { CaptionTimingMap, CaptionTimingEntry } from "../../content/composition/generate-caption-timing-map.js";

export function resolveActiveCaption(
  captionMap: CaptionTimingMap | undefined,
  frame: number
): CaptionTimingEntry | null;
```

HUD stack resolver (extend resolve-modules.ts):
```typescript
export type HudModuleId =
  | "viz-hud-actor-label"
  | "viz-hud-beat-caption"
  | "viz-hud-packet-id"
  | "viz-hud-frame-counter";

export function resolveHudModules(
  vizFrameState: VizFrameState,
  sceneSpec: SceneSpec,
  options?: { captionMap?: CaptionTimingMap; showFrameCounter?: boolean }
): HudModuleId[];

// Extended VizModuleStack:
export type VizModuleStack = {
  primary: string[];
  cert: CertModuleId | null;
  hud: HudModuleId[];
  zOrder: string[]; // full back → front including HUD
};
```

Compose plan options:
```typescript
export type ComposeOptions = {
  captionMap?: CaptionTimingMap;
  showFrameCounter?: boolean;
};

export function getComposePlan(
  sceneSpec: SceneSpec,
  frame: number,
  options?: ComposeOptions
): ComposePlan;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Caption resolver and HUD R3F components</name>
  <read_first>
    - src/content/composition/generate-caption-timing-map.ts
    - docs/r3f-module-catalog.md (HUD Modules)
    - src/client/viz/style-tokens.ts
  </read_first>
  <files>
    - src/client/viz/resolve-hud-caption.ts
    - src/client/viz/hud/viz-hud-actor-label.tsx
    - src/client/viz/hud/viz-hud-beat-caption.tsx
    - src/client/viz/hud/viz-hud-packet-id.tsx
    - src/client/viz/hud/viz-hud-frame-counter.tsx
    - src/client/viz/registry.ts
  </files>
  <action>
    Implement resolveActiveCaption: find entry where frame >= startFrame && frame <= endFrame (first match).
    Create four HUD components accepting props (actors, activeCaption, packets, frame) — use @react-three/drei Html or billboard group; text colors from style tokens only.
    Register HUD_MODULE_IDS and PHASE_18_MODULE_IDS (all 10 catalog ids) in registry.ts.
  </action>
  <acceptance_criteria>All HUD ids registered; caption resolver deterministic.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-cert-hud-modules.test.ts --testNamePattern="resolveActiveCaption|hud"</automated>
  </verify>
  <done>HUD module family implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Full z-order compose and catalog doc</name>
  <read_first>
    - src/client/viz/compose-scene.tsx
    - docs/r3f-module-catalog.md
  </read_first>
  <files>
    - src/client/viz/resolve-modules.ts
    - src/client/viz/compose-scene.tsx
    - docs/r3f-module-catalog.md
  </files>
  <action>
    Extend resolveVizModuleStack → resolveFullModuleStack (or extend in place): append hud modules after cert layer. HUD rules: actor-label when actors.length > 0; beat-caption when resolveActiveCaption non-null; packet-id when packet timeline active; frame-counter when showFrameCounter option true.
    Update getComposePlan and VizScene to accept ComposeOptions; renderOrder = tunnel → packet → cert → hud.
    Add viz-hud-beat-caption row to catalog HUD table (beat caption from CaptionTimingMap.scriptIntent).
  </action>
  <acceptance_criteria>Compose plan includes HUD layer; catalog documents beat-caption id.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-cert-hud-modules.test.ts --testNamePattern="compose|zOrder"</automated>
  </verify>
  <done>Full four-family composition API complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VIZ-04 integration tests and catalog coverage</name>
  <read_first>
    - tests/viz-cert-hud-modules.test.ts
    - tests/viz-packet-tunnel-modules.test.ts
  </read_first>
  <files>
    - tests/viz-cert-hud-modules.test.ts
  </files>
  <behavior>
    - Test 1: resolveActiveCaption returns correct beat on TLS caption map frame.
    - Test 2: resolveHudModules includes viz-hud-beat-caption when caption active.
    - Test 3: getComposePlan renderOrder ends with HUD modules after cert/packet/tunnel.
    - Test 4: VIZ_REGISTRY keys match all 10 catalog viz-* ids from r3f-module-catalog.md.
    - Test 5: HUD component sources contain no hardcoded hex.
    - Test 6: Full test suite (viz-cert-hud + viz-packet-tunnel) green.
  </behavior>
  <action>Extend tests with describe "VIZ-04 HUD modules".</action>
  <acceptance_criteria>VIZ-04 and full catalog coverage verified.</acceptance_criteria>
  <verify>
    <automated>npm run test -- tests/viz-cert-hud-modules.test.ts tests/viz-packet-tunnel-modules.test.ts</automated>
  </verify>
  <done>Phase 18 full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captionMap → HUD | Read-only timing entries |
| HUD → scene layers | Overlays must not mutate frame-state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | D | caption frame boundary | mitigate | Inclusive start/end frame test |
| T-18-04 | I | missing catalog id | mitigate | Registry vs catalog key parity test |
</threat_model>

<verification>
- [ ] `npm run test -- tests/viz-cert-hud-modules.test.ts`
- [ ] `npm run test -- tests/viz-packet-tunnel-modules.test.ts`
- [ ] `npm run validate:requirements` (after marking VIZ-03/04 complete in execution)
</verification>

<success_criteria>
- VIZ-04: HUD modules display actor labels and beat captions aligned to caption timing maps.
- ROADMAP criterion 3: All catalog viz-* ids have R3F implementations.
</success_criteria>

<output>
After completion, create `.planning/phases/18-r3f-certificate-hud-modules/18-02-SUMMARY.md`
</output>
